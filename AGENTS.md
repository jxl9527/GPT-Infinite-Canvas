# D5渲染工作流测试项目操作规则

## 画布代码唯一位置

- 当前画布工程的唯一维护位置为 `D:\OneDrive - 静超科技\D5-AI-Pipeline\canvas`。
- D5 管线根目录为上一级 `D:\OneDrive - 静超科技\D5-AI-Pipeline`。
- 后续画布源码、扩展、桥接服务、安装脚本和文档修改必须在本目录内完成。
- 迁移前的旧 `GPT画布` 目录只作为只读备份，不再写入新改动。

## 接手顺序

1. 先读本目录 `README.md` 和本文件。
2. 修改画布代码、工作流或发布包前，必须完整阅读 `docs\已修复问题与回归保护基线.md`。
3. 再读对应 CodexWorkspace 任务：`D:\CodexWorkspace\02_任务\2026-07-30_D5渲染工作流测试项目_投标支持`。
4. 处理具体文件前，确认当前工作版、源文件和输出位置。

## 画布版本回归保护

- 当前已验证的 105 项测试只保护稳定底座，不覆盖阶段一、阶段二完整业务流程；具体能力、缺口和检查命令以 `docs\已修复问题与回归保护基线.md` 为准。
- 修改阶段一、阶段二前必须先补最小行为刻画测试；暂时无法自动化的交互必须保存人工回归用例和预期结果。
- 后续版本不得通过删除测试、跳过测试或减弱断言来掩盖基础功能回退。
- 项目保存与恢复、撤销重做、图片内存管理、任务恢复、重复提交保护、登录与本地安全、文字卡、项目导航和发布包完整性属于稳定底座。
- 工作流语义变更可以替换旧测试，但必须先补充等价的新测试，并记录旧测试、新测试及其保护能力之间的映射。
- 开发前先保留当前工作区状态和测试结果；发布前必须完成自动测试、旧项目副本迁移验证和人工冒烟检查。
- 任一基础能力回退都属于发布阻断，不得制作或交付正式更新包。

## V3 画布边界

- 2026-09-11 用户批准简化新版，适用于 `agent/canvas-simple-prompt-library`：以 `docs/无限画布简化改版项目计划书.md` 和用户后续确认取代下方 V3 主流程语义。新版为固定提示词图库＋图片画布，网页生成默认双任务并发、可退回单任务；不强制三阶段、先提词、五项验收或玻璃导出。下方 V3 条款只适用于旧版兼容入口。保留原始数据、保存恢复、版本引用、提交防重、结果归属、登录与本地安全等基础保护，并补充新行为测试。

- V3 画布以 `docs\画布V3工作流优化项目书.md` 为工作流语义的唯一依据；下方“效果图工作流”仅约束画布之外的项目文件组织，不得反向恢复画布旧版四阶段门禁。
- 画布固定为“前置阶段—优化阶段—最终阶段”三段；允许用户从任一阶段直接开始，不得用跨阶段禁止、前置完成状态或玻璃质量门阻断操作。
- 最终阶段只处理用户导入的 D5 完整图，执行无蒙版整图玻璃深化、人工验收、版本选定和批量导出；不上传 SU、通道图或蒙版。
- Photoshop 为画布之外的纯人工流程。选定玻璃深化图成功导出至用户指定文件夹后，画布任务即结束；不得在画布中创建 PS 母版、PS 目录或后期任务。

## 目录规则

- 项目根目录只保留 `README.md`、`AGENTS.md` 和 `01投标阶段`。
- `01投标阶段`固定使用：`0基础资料`、`CAD`、`SU`、`文本`、`分析图`、`效果图`、`参考案例`。
- 不预建无实际用途的中间目录；需要时再建立名称明确的子目录。
- Codex 临时文件、预览、测试、过程日志和项目专用脚本保存在对应的 `D:\CodexWorkspace\02_任务`任务内。
- 可跨项目复用的脚本、模板和素材保存到 `D:\CodexWorkspace\04_工具与资源`。

## 效果图工作流

- 用户提出“开始效果图”后，先根据本项目 `README.md` 确认正式项目路径，再定位 `01投标阶段\效果图`；不得在项目外另建效果图项目目录。
- 效果图目录按生产阶段和 D5 渲染批次组织，不为每个视角建立一套独立文件夹。
- 用户开始第一次 D5 批量渲染时，只创建 `D5_01`目录，并将 D5 渲染队列的统一存储位置指向该目录。
- 后续重新渲染或补渲时，新建 `D5_02`、`D5_03`等批次目录；不得覆盖或混入之前批次的原始输出。
- D5 场景名称直接作为后续文件识别依据，推荐采用：`1人视`、`2鸟瞰`、`3沿街`。只有建筑被树木、人物或车辆明显遮挡，或重要入口、复杂幕墙需要后期恢复时，才增加 `1人视_净`这类净场景；不得机械地为每个视角复制净场景。
- SketchUp 只负责准确建模、材质分类和向 D5 同步模型，不作为固定流程输出线稿、AO、白模或最终相机图。
- 最终相机、焦距、画幅、构图、光影和配景在 D5 中确定。
- 同一 D5 批次应使用统一的画幅比例、输出分辨率、图片格式和通道设置；不同交付版式确需采用不同参数时，应拆分为不同批次。正式批次原则上使用 4K PNG，避免在同一正式批次中混用 2K和 4K。
- D5 默认输出主渲染图、AO、Material ID和 Transparent透明通道。Sky Mask、Z-Depth、Reflection等通道按实际后期需要增加；AI Albedo、AI Normal及全部通道图不作为默认输出。
- 同一批次的全部视角、主图和通道图统一保存在对应的 `D5_XX`目录中，保留 D5 原始文件名和通道后缀，不手动拆分到视角目录。
- 进入 AI 阶段时同时创建统一的 `AI`和`成图`目录。`AI`用于保存全部视角的提示词和AI版本，`成图`用于后续保存最终成果；不再将两者分阶段创建。
- AI版本通过简短文件名区分视角，例如：`1人视_01.png`、`1人视_02.png`、`1人视_提示词.txt`。
- AI生成默认只上传一张 D5完整渲染图。只有重要建筑结构被配景明显遮挡时，才同时上传对应的 D5净图，并在提示词中明确：完整图决定构图、光影、材质、植物和环境，净图只用于确认被遮挡的建筑结构。
- AO、Material ID、Sky Mask、Z-Depth、Reflection、Normal、Transparent及 SU线稿不得作为默认 AI参考图；这些文件保留给 Photoshop后期使用。
- 每个视角默认先生成 2个 AI版本。选图顺序固定为：建筑体块与层数、门窗和幕墙、道路与场地、光影材质、植物氛围。主体结构明显错误的版本应重新生成，不得仅因氛围好看而进入 Photoshop，也不得依赖 Photoshop大面积抢救。
- 用户确认AI版本后，不复制或另存 `视角名_选定.png`。Codex应将“视角—采用版本—原始AI文件路径—对应D5批次”写入对应Codex任务日志中的版本化选图记录，Photoshop直接引用原始AI版本。
- Photoshop 不建立单独的 `PS`目录，也不按视角建立后期目录。PSD、PSB及相关后期工作文件直接保存在 `效果图`根目录，例如：`1人视.psd`、`2鸟瞰.psd`，减少目录层级，便于打开。
- Photoshop母版固定使用以下简化结构：

```text
03_修复
├─ 局部修补
├─ 透明通道_Transparent
├─ AO
└─ D5净图_结构恢复（可选）

AI采用版本
D5完整图（锁定）
```

- `D5完整图`作为最底层结构保底，直接位于PSD外层并自动锁定；`AI采用版本`也直接位于外层，引用选图记录中的原始AI文件，位于D5上方并添加白色蒙版；存在净图时，将 `D5净图_结构恢复`置于`03_修复`并添加全黑蒙版，按需恢复窗框、入口、屋顶、栏杆、台阶和道路边界。
- 不自动创建`04_调色`、`02_AI`或`01_D5`组。调色层由设计师根据每张图的实际需要手动创建。
- `透明通道_Transparent`作为隐藏辅助图层置于 `03_修复`，默认关闭可见性；需要增强玻璃内透时，用于建立玻璃选区，不直接参与最终合成。
- AO默认采用正片叠底，建议不透明度为 8%～15%，并按需添加蒙版；Material ID、Sky Mask、Z-Depth等其他通道不作为固定可见图层置入母版。
- 母版PSD不固定建立 Grain、Sharpen、Export Check、Reference或 SU Line组。最终锐化必须在成图尺寸确定后，对输出副本处理，不得在分层母版中预先固定。
- `成图`目录已在进入AI阶段时与 `AI`目录同时创建，用于保存全部确认后的 JPG、PNG、TIFF等交付文件。
- 创建 Photoshop母版前，Codex应根据版本化选图记录生成PS母版任务，核对D5完整图、AI采用版本、AO、Transparent及可选净图的路径和尺寸。Photoshop通过 `D5 AI PS母版` UXP插件读取任务并创建PSD；已有同名PSD时使用 `_v02`、`_v03`，不得覆盖。
- 推荐结构如下：

```text
01投标阶段/
└─ 效果图/
   ├─ D5_01/
   │  ├─ 1人视.png
   │  ├─ 1人视_净.png
   │  ├─ 1人视_AO.png
   │  ├─ 1人视_MaterialID.png
   │  ├─ 1人视_Transparent.png
   │  ├─ 2鸟瞰.png
   │  ├─ 2鸟瞰_AO.png
   │  ├─ 2鸟瞰_MaterialID.png
   │  └─ 2鸟瞰_Transparent.png
   ├─ AI/
   │  ├─ 1人视_01.png
   │  ├─ 1人视_02.png
   │  ├─ 1人视_提示词.txt
   │  ├─ 2鸟瞰_01.png
   │  └─ 2鸟瞰_02.png
   ├─ 1人视.psd
   ├─ 2鸟瞰.psd
   └─ 成图/
      ├─ 1人视.jpg
      └─ 2鸟瞰.jpg
```

- 上述目录均按实际进度创建；开始效果图任务时不得预建 `AI`、`成图`或未来的 D5 批次空目录。进入AI阶段时，`AI`与`成图`必须同时创建。
- Codex 在 D5 批量渲染完成后，可根据场景主文件名和通道后缀自动识别视角、主图、净图、AO、Material ID及 Transparent，不移动、不重命名原始 D5 输出。
- 存在多个 D5批次时，不得简单混用最高编号中的全部文件。Codex应以原完整批次为基础，逐视角识别后续同名补渲，并在对应 Codex任务日志中记录实际采用的批次，例如：`1人视采用 D5_02`、`2鸟瞰采用 D5_01`；存在歧义时先确认。
- 新增视角时优先加入下一次 D5 批量渲染，不调整已有视角文件；所有同名衍生文件自动增加版本号，不覆盖已有成果。
- 高清输出顺序固定为：完成PS合成与调色、保存分层母版、生成输出副本、按目标尺寸放大、在最终尺寸锐化、输出到 `成图`目录。不得将 AI生成图直接视为最终印刷文件。
- Codex 测试图、技术预览、调试缓存、脚本和过程日志仍保存在对应的 `D:\CodexWorkspace\02_任务`任务内，不放入正式效果图目录。

## 文件安全

- 不删除项目原始资料，不覆盖同名文件。
- 修改 Office、InDesign、PDF、图片、CAD 或 SU 文件前先复制或另存。
- 批处理输出到独立目录并生成日志。
- 不拆散 InDesign 打包文件夹，不单独移动其中的 `Links`、`Document fonts`、INDD、IDML 或 PDF。
- 移动外部链接图片、PSD、PDF 或图标前，先检查 InDesign 链接路径。
- SketchUp 模型与其贴图目录保持配套关系。

## 建筑表达

- 默认中文输出，面向投标文本、方案汇报和分析图表达。
- 图文遵循“图多文少、主图放大、信息分层、少即是多”。
- 图像处理必须保护建筑结构、体块关系、轴测视角、道路关系和场地边界。
- 当图一仅为风格参考、图二为结构依据时，严禁改变图二的造型、数量、比例、朝向、视角、层级和空间关系。

## 成果流转

- Codex 初步成果先进入对应任务目录复核。
- 确认采用后，再复制到 `01投标阶段`的对应目录。
- 最终分析图进入 `分析图`，模型进入 `SU`，文本和打包文件进入 `文本`，效果图进入 `效果图`。
