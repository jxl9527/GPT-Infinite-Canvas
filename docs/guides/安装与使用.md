# 安装与使用

## 1. 准备环境

安装 Node.js 24 或更高版本，并确认 PowerShell 7 可用：

```powershell
node --version
pwsh --version
```

## 2. 安装依赖并构建

```powershell
cd app
npm install
npm run build
npm run build:canvas
```

## 3. 启动画布

双击：

```text
app\05_installer_and_ops\Start-GPTCanvas.vbs
```

浏览器将打开 `http://127.0.0.1:3230`。

### 固定使用 Chrome 的桌面入口

如果 Windows 默认浏览器是 Edge，但希望无限画布始终使用 Chrome，运行：

```powershell
pwsh app\05_installer_and_ops\Install-GPTInfiniteCanvas-DesktopLauncher.ps1
```

如果使用 GitHub Release 中的安装包，解压后可以直接双击：

```text
05_installer_and_ops\Install-GPTInfiniteCanvas-DesktopLauncher.cmd
```

桌面将出现“GPT 无限画布”快捷方式。它会：

1. 检查并启动本地桥接服务。
2. 等待画布服务就绪。
3. 明确调用 Google Chrome 新窗口打开画布。
4. 保持系统默认浏览器设置不变。

如果桌面已经有同名快捷方式，安装脚本会先在桌面创建带时间戳的备份，再更新入口。

## 4. 加载 Chrome 浏览器扩展

1. 在 Chrome 中打开 `chrome://extensions/`。
2. 开启“开发者模式”。
3. 选择“加载已解压的扩展”。
4. 指向 `app\03_browser_extension\dist`。
5. 在扩展弹窗中连接本地桥接服务。

每次点击扩展管理页的“重新加载”后，需要刷新已经打开的画布和 ChatGPT 页面，让新版内容脚本重新连接。

### 本地桥接令牌

正式运行令牌位于：

```text
app\02_bridge_service\runtime\bridge-token.txt
```

项目登记页右上角提供“复制桥接令牌”按钮。该按钮直接读取当前服务使用的令牌，是推荐方式。

不要复制 `output`、`.test-dist` 或 `playwright` 测试目录中的同名文件；这些令牌只属于隔离测试服务，粘贴到正式扩展会提示无效。令牌仅供本机扩展连接，请勿截图、上传或提交到 GitHub。

## 5. 创建任务

1. 输入项目名称；项目需求 Markdown 为可选项。
2. 创建项目后，拖入图片或点击左侧“导入”。
3. 为图片分配结构基准、风格参考或编辑目标。
4. 添加必要批注。
5. 展开“GPT 工作台”，选择“分析图片／生成或优化提示词／按提示词生成图片”。普通 GPT 默认可直接使用。
6. 在任务预览中填写本轮修改要求；如使用已验证的专属 GPT，再主动启用并填写其首页地址。
7. 确认最终提示词和附件身份后主动发送。文字结果会回到原图右侧，可编辑、选择并继续发送。

### 批量阶段处理

1. 使用默认选择工具，在画布空白处直接按住左键拖出选择框；也可以按 `M` 或点击左侧“批选”。
2. 在右侧选择统一阶段和本轮动作，可以只更新画布状态，也可以发送到普通 GPT 或已启用的专属 GPT。
3. 分析与提示词动作返回文字卡；生图动作返回完整图片。阶段三推荐先批量生成提示词，人工选取或微调后再批量生图。
4. 队列严格逐张执行；登录、验证、比例异常或生成失败时会暂停，不重复提交。

专属 GPT 是可选项。启用并确认地址后会固定在本机并跨项目复用；已经开始的批次会固定使用启动时的目标。

### 固定工作流提示词

“GPT 工作台”内置九张不可删除的固定卡：构图与相机审查、SU可见细节审查、有参考图场景提示词、无参考图场景提示词、粗提示词优化、场景目标图生成、最终D5整图玻璃深化、继续当前项目、阶段验收与推进。点击“套用”会同时切换合适的阶段与动作，并将完整提示词填入任务预览。

GPT返回的分析和提示词会作为文字卡放在来源图右侧。文字卡支持直接编辑、框选一段用于下一步、使用全文、复制以及存入项目提示词库；移除文字卡不会删除GPT原始任务记录。

### 阶段四：整图玻璃深化

阶段四只输入最终D5完整效果图，不输入上下层蒙版。提示词控制下部和首层以克制暖色内透为主，上部以蓝灰天空与环境冷色反射为主。GPT必须返回同宽高比、同裁切的完整效果图，不能返回局部玻璃素材。画布会拒绝比例不同的结果；比例相同但像素不同，会生成与原D5像素一致的 `_PS对齐` 副本。用户在PS中自行抠取玻璃区域叠加。

### 采用与正式归档

画布上的“标记采用”只登记当前视角的采用候选。需要写入正式项目时，展开输入区的“采用归档”，明确设置 `效果图\AI` 绝对路径，再执行归档。系统会同时核对同级“成图”目录、自动顺延版本号、保存对应提示词并记录日志；不会扫描其他项目，也不会覆盖已有文件。

### 移除项目

在项目登记簿中点击“删除”，再点击“确认移除”。项目会移动到本地 `runtime/trash/projects` 回收目录，不会立即永久删除；兼容入口“原有项目”不能在工作台中删除。

常用键盘操作：

- `Ctrl+Z`：撤销。
- `Ctrl+Y` 或 `Ctrl+Shift+Z`：重做。
- `M`：进入批量框选。
- 方向键：移动当前图片或批注 1 个单位。
- `Shift + 方向键`：移动 10 个单位。

生成出现登录、网页验证或扩展异常时，可以点击“结束当前任务”。如果任务已经发送到 ChatGPT，此操作只结束本地跟踪，不能撤回已发送内容。

停止服务时，双击 `app\05_installer_and_ops\Stop-GPTCanvas.vbs`。
