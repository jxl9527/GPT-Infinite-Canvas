# 2026-09-13 图片对比与多版本三并发实施

依据：[图片滑动对比与多版本三任务并发计划书](../图片滑动对比与多版本三任务并发计划书.md)（实施对象 OpenCode）。本记录写清本轮实际完成与验证，未做项已注明。

## 1. 本轮目标与结论

按计划书第 6 节实施三项能力：同图同提示词多版本、最多三任务并发、点击生成图滑动对比。三项代码均已实现，自动测试通过；浏览器交互、真实生成与用户验收未做。

## 2. 完成事项

### 2.1 多版本模型（计划第 2 步）

- `simple-state.ts`：`SimplePreferences.copiesPerImage`（1/2/3，默认1）；`makeSimpleBatch` 按轮次展开 `A1、B1、A2、B2`，每项独立 `item.id` 与稳定 `variantOrdinal`；展开后超过 200 个任务明确报错。
- `appendSimpleVariant`：活动批次内追加，复用批次冻结提示词，编号续增，记录 `clonedFromItemId`；暂停中追加保持排队。
- `retrySimpleItem`：失败/停止项补生改为新建 item（`retryOfItemId`），旧项标记 `supersededByItemId` 保留记录；`nextSimpleItem` 与 `batchFinished` 跳过已替代项。
- 占位：`pendingSlots` 按同源待处理顺序分配槽位；`simpleSlotBounds` 计算落点；`appendImage` 新增 `bounds`，与现有节点重叠时回退 `nextChildBoundsToRight`。占位与队列显示 `V序号`。
- UI：设置面板新增“每张图生成 1/2/3 个版本”；渲染面板显示“共 N 个任务，最多 C 个同时生成”；运行中项提供“同样设置再生成”；失败项按钮由“重新生成”改为“同样设置再生成”；进度分母排除已替代项。
- 多结果：同一任务返回多图时按 `_1`、`_2` 命名，全部保留，`taskId+resultId` 幂等。

### 2.2 三任务并发（计划第 3 步）

- 共享类型新增 `SimpleRenderConcurrency = 1 | 2 | 3`，`parseSimpleRenderIdentity` 接受 1—3、拒绝 0/4。
- 桥接项目校验接受并发 3；`TaskStore` 容量逻辑保持“已占槽计入、超过并发即拒绝”，新增三并发服务锁测试。
- 桥接健康声明 `simpleRenderConcurrency: 3`；前端启动检查接受 2 或 3，桥接为旧版（2）时隐藏“3 个任务”选项。默认仍为 2，旧项目设置不变，活动批次中不可更改。

### 2.3 滑动对比（计划第 4 步）

- 新增 `comparison-model.ts`：严格按 `parentVersionId` 配对；`rootBaseVersionId` 支持“最初原图”；`comparisonSiblings` 提供同父版本切换；比例差 >1% 提示“画幅比例不同”。
- 新增 `ComparisonOverlay.tsx`：左“本次底图”、右“生成结果 · V编号”的 0—100% 滑块；键盘方向键（Shift 加速）、Home/End、Esc、Tab 焦点限制；滚轮缩放、中键平移、适合窗口与 100%；指针松开与窗口失焦兜底；加载中/失败/重新载入状态；缺少父版本或资产时降级单图预览并提示“缺少对比底图”。
- `BasicCanvas`：选择工具下单击生成的图片（移动 ≤5px、无 Shift/Ctrl、非拖动）延迟 240ms 打开对比；双击预览保留并在双击时取消单击计时；工具栏新增“与原图对比”；`onPreview` 改传 `{assetId,name,versionId}`。
- 对比打开不进入撤销栈，不改变画布视口与选择；关闭后恢复焦点。

## 3. 验证结论

- **代码已实现**：2.1、2.2、2.3 所列文件。
- **自动测试通过**：`npm --prefix app run check`、`npm --prefix app run check:canvas`、`npm --prefix app test` 全通过；全仓 **193**（共享 4、桥接 31、扩展 13、画布 145），0 失败。本轮新增 11 项测试，并同步修正 2 项既有断言（三并发合同、越界保存值 3→4）。
- **构建产物**：`app/01_canvas_app/dist` 已含新功能文案（同样设置再生成、与原图对比、每张图生成、3 个任务、本次底图、画幅比例不同、缺少对比底图）。
- **浏览器实测**：已做（用户操作、Codex 核对）。设置出现 1/2/3 与每图 1/2/3、任务总数显示正常；单击生成图打开对比，滑块拖动、方向键、Home/End、滚轮缩放、中键平移、Esc 均可用；单击导入图不打开对比；双击生成图只出现原尺寸预览（首次发现双击会跳到对比，已修复：抬手时在 250ms 内识别同图第二次单击并取消对比计时，新增 `basicCompareClick` 纯函数与测试）；工具栏“与原图对比”可用。
- **真实任务回图**：已做，三组全部通过（详见第 8 节）。
- **用户验收**：用户目视确认界面与流程无问题；生成图的画质与建筑结构未逐张评审。

## 4. 未完成与限制

- 旧项目（无 `copiesPerImage`、并发 1/2）打开后保存的迁移冒烟未做；运行时缺字段按默认值处理。
- 刷新不额外生图、以及第二个/第三个网页刷新或扩展重载后的恢复未单独实测。
- “一次任务返回多结果”未构造真实场景验证子序号。
- `variantOrdinal` 按当前批次内续增；新批次会重新从 1 编号，跨批次编号不连续，显示以“生成结果 · V编号”为准。
- 并发档位在活动批次中不可更改，切换需等批次结束。

## 5. 下一步建议（优先级）

1. **旧项目迁移冒烟**：打开旧 1/2 并发项目，确认默认值与保存往返，冲突不覆写（零生成成本）。
2. **刷新防重与恢复**：生成中刷新画布确认不重发；网页刷新/扩展重载后已提交任务恢复观察（约 1—2 次生成）。
3. **异常与提交**：本轮双击修复的代码提交（`BasicCanvas.tsx`、`basic-canvas-model.ts`、测试）与文档同步；提交前跑全仓测试。
4. **发布**：不在本轮范围；按后续授权与 AGENTS 执行。

## 6. 关键文件与命令

- 新增：`app/01_canvas_app/src/comparison-model.ts`、`app/01_canvas_app/src/ComparisonOverlay.tsx`、`app/01_canvas_app/test/comparison-model.test.ts`。
- 修改：`simple-state.ts`、`use-simple-project.ts`、`SimpleCanvas.tsx`、`BasicCanvas.tsx`、`simple.css`、`bridge-client.ts`、`test/simple-state.test.ts`、`test/basic-canvas-model.test.ts`、`app/04_shared_packages/src/index.ts`、`app/02_bridge_service/src/{server.ts,canvas-project-repository.ts}`、桥接测试 `simple-concurrency.test.ts`、`simple-persistence.test.ts`，以及计划书状态与回归保护基线。
- 检查命令：

```powershell
npm.cmd --prefix app run check:canvas
npm.cmd --prefix app run test:canvas
npm.cmd --prefix app test
git diff --check
```

## 7. 现场状态

- 分支 `agent/canvas-simple-prompt-library`，HEAD `7aa67ff`；已提交：代码 `74feecf`、文档 `7f663db`/`7aa67ff`。**未提交**：测试中发现的双击修复（`BasicCanvas.tsx`、`basic-canvas-model.ts`、`test/basic-canvas-model.test.ts`）及本文档本轮更新。
- 服务已用正式启动器（pwsh）重启为新构建：`http://127.0.0.1:3220/health` 返回 `simpleRenderConcurrency: 3`，UI 3230 返回 200。
- 真实项目 `project_fca8456c…` 在本轮测试中被真实写入：revision 2618、节点 26；新增任务与版本均保留，未删除任何数据。
- 提示词草稿里仍留有测试句 `（草稿修改测试，不应进入任务）`，下次真实生成前需清除。
- 扩展未改动；画布页面已按新构建刷新。

## 8. 真实与交互验收记录（2026-09-13 下午）

### 8.1 同图三份（并发 3）

- 批次 `simple_eb011b4e…`；底图 `6转角透视2_1.png`（`version_91121bfa…`），每图 3 版。
- 任务 `48c47776`（V1）/`54cbf678`（V2）/`5adacb3c`（V3），提交 15:26:57 / 15:27:03 / 15:27:10，完成 15:28:26 / 15:28:19 / 15:28:25。
- 15:27:10—15:28:19 三个任务同时 generating（约 69 秒重叠）；乱序完成，3 个新节点全部挂在同一父版本，节点 16→19，每任务 1 结果，无 handoff、无重复。

### 8.2 三图各一份（并发 3）

- 批次 `simple_beeff98d…`；底图 `2人视2_1`（`version_59ad4058…`）、`4半鸟瞰2_1`（`version_3d89d998…`）、`5转角透视_1`（`version_1e9aa418…`），每图 1 版。
- 任务 `d1541d16`/`e4a3cb4d`/`4952180b`，提交 15:32:26 / :31 / :38，完成 15:33:27 / 15:33:49 / 15:33:33。
- 15:32:38—15:33:27 三任务重叠（约 49 秒）；乱序完成，三个结果各自挂在对应底图，节点 19→22，无串图、无 handoff。

### 8.3 活动中追加、冻结提示词与第四项排队（并发 3）

- 批次 `simple_5578755a…`；底图 `6转角透视2_1.png`，先建 1 项，生成中追加 3 次。
- V1 `66e25370`（创建 15:37:03，提交 15:37:09，完成 15:38:09）；V2 `f42bc1b6`；V3 `2811984f`；V4 `976ac82a`。
- 追加项 V2/V3/V4 均记录 `clonedFromItemId`；V4 在 V1 完成后（15:38:10）才创建、15:38:15 提交，证明第四项等待名额并在释放后自动补位；全程最多 3 个并发。
- 冻结验证：追加期间在草稿末尾加入测试句；批次 `prompt` 与 V1—V4 四个任务的 prompt 均不含该句（`promptFrozen` 全部为真）。
- 4 个新节点 parent 全部正确，节点 22→26。

### 8.4 交互验收

- 设置面板 1/2/3 与每图 1/2/3、任务总数文案正常；单击生成图打开对比，滑块、方向键、Home/End、滚轮缩放、中键平移、Esc 可用；导入图不触发对比。
- 首次发现双击生成图会先预览后跳对比；修复后用户复测确认“只出现原尺寸预览”。修复同时新增 `basicCompareClick` 纯函数与单元测试。

### 8.5 判定

- 计划第 7 节中“点击生成图、滑块与缩放、同图双次、活动中追加、重放防重（幂等由测试覆盖）、三个同图任务、三个不同图任务、四个以上任务等待、保存字段校验、原能力回归（自动测试）”均已获得证据。
- 仍未验证：旧项目迁移冒烟、生成中刷新/网页刷新/扩展重载恢复、真实多结果子序号、结果画质逐张评审。
