# 2026-09-13 生成入口提示与官方模板接入

承接：[AI 接手交接文档](../AI接手交接文档.md) 第 8 节。本记录只写本轮实际完成与验证，未做的检查已注明。

## 1. 本轮目标与结论

用户要求“查看 AI 接手交接文档接着往下推进”，并勾选三条线索：遗留任务/TASK_LOCKED 入口、真实双任务验收、官方提示词模板对话。真实双任务验收经用户选择**推迟**。

结论：

- 完成优先项 5：无扩展时“重新生成”与“开始渲染”提示一致并同样禁用。
- 完成遗留任务入口：不再静默吞掉 `TASK_LOCKED`，队列显示占用原因并在含“旧版”时给出旧版入口。
- 官方模板对话内容已从会话日志恢复（无需再联网）；按其判断把 5 张效果模板改写为 GPT Image 2.5 官方骨架并加入内置图库（6 张变 8 张）。
- 固化“每轮结束写交接记录”的要求，写入 `AGENTS.md`。

## 2. 完成事项

### 2.1 无扩展时“重新生成”一致性

- `app/01_canvas_app/src/SimpleCanvas.tsx`：抽出共用 `connectionHint`，“开始渲染”和“重新生成”共用；未连接或版本低于 1.6.0 时禁用“重新生成”，按钮 `title` 与队列内 `role=status` 说明原因。

### 2.2 TASK_LOCKED 不再静默、旧版入口可见

- `app/01_canvas_app/src/simple-state.ts`：新增纯函数 `simpleBlockingTask(batch, tasks)`，识别不属于当前批次且未终结的任务。
- `app/01_canvas_app/src/use-simple-project.ts`：新增 `lockNotice`；创建任务前命中阻塞项即写入提示并暂缓创建，创建成功或批次结束清空；竞态下捕获 `TASK_LOCKED` 显示服务端原文。
- `app/01_canvas_app/src/SimpleCanvas.tsx`：队列显示 `lockNotice`，含“旧版”时附“打开旧版兼容工作台”；`openLegacy` 提取为共用函数。
- 未改桥接占用规则与排程算法。

### 2.3 官方提示词模板接入

- 来源：对话 `6aa3572c-2e88-83ec-9e25-ea92458edd40`《查找官方模板》内容在 2026-09-11 Codex 会话日志中已恢复，存档于 `D:\CodexWorkspace\02_任务\2026-09-13_官方提示词模板结合评估`（含《建筑专用Prompt手册_对话恢复.md》《上一轮适配判断.md》）。
- 事实边界：15 套模板是依据 OpenAI GPT Image 2.5 Prompting Guide 改写的建筑适配版，不是官方直接发布的建筑模板。
- 按上一轮适配判断落地第一批 5 张效果模板：明亮晴空 · 清透写实、D5 质感强化、玻璃质感优化、阴天柔光、蓝调夜景；统一 `REFERENCE / CHANGE ONLY / PRESERVE EXACTLY / VISUAL TARGET / CONSTRAINTS` 骨架，结构保护写入提示词，复制即为自包含完整提示词；玻璃卡只改玻璃并保留天气、材质分区、景观与相机。
- `app/01_canvas_app/src/prompt-starters.ts`：内置 6 张变 8 张；分析图、总平、室内三张沿用。
- `app/01_canvas_app/src/prompt-library-item.ts`：新增；`PromptLibraryItem` 类型移到节点安全模块，`bridge-client.ts` 改为 re-export，避免测试引入 `import.meta.env`。
- 同步文档：`提示词内容包说明.md`、`无限画布简化改版项目计划书.md`、`简化版验收说明.md`、`已修复问题与回归保护基线.md`、`AI接手交接文档.md`。

### 2.4 交接流程固化

- `AGENTS.md` 新增“交接记录要求”：每轮结束必须在 `docs\交接记录\` 新建记录，写清完成/验证（区分代码、自动测试、浏览器实测、真实回图、用户验收）/遗留/下一步，并在 `AI接手交接文档.md` 顶部登记。

## 3. 验证结论

- **代码已实现**：2.1、2.2、2.3 所列文件均已改完。
- **自动测试通过**：`npm --prefix app run check:canvas`、`npm --prefix app run test:canvas`、`npm --prefix app test` 全通过；全仓 **182**（共享 4、桥接 30、扩展 13、画布 135），0 失败；`git diff --check` 干净。本轮新增 3 项测试：`占用生成名额的非本批任务被识别为锁，完成或取消后解除`、`内置图库首批五张效果模板为自包含完整提示词`、`图库模板标题与提示词长度满足导入与卡片约束`。
- **浏览器实测**：未逐项实测。仅用构建产物确认 `app/01_canvas_app/dist` 含新提示文案与 5 个新模板标题；未在浏览器点击“重新生成”禁用态，也未复现旧任务占用的 `lockNotice`。
- **真实任务回图**：已做（同日追加，详见第 8 节）。双任务机制走通：两个独立网页、提交串行、生成重叠、乱序完成、各自挂到正确原图、各 1 张无重复；但第一张任务的扩展标签页绑定在第二张提交瞬间失效，自动回收被拒，需人工“重新绑定当前任务”后手动检查结果才回收，属阻断级缺陷。
- **用户验收**：未完成。画质/结构初查见第 8 节，需用户判定是否采用。
- 已提交并推送：commit `0563c23`（23 文件、814 插入 / 148 删除），`origin/agent/canvas-simple-prompt-library` 已同步。未制作发布包。

## 4. 未完成与限制

- **未解决缺陷**：并发打开第二个 ChatGPT 网页时，第一个任务的扩展绑定失效（`页面消息与绑定任务或标签页不一致`），自动回收与自动 handoff 均失败；无人值守时会永久停在 submitted。根因未定位，第一张靠人工重绑兜底。详见第 8 节。
- 本轮两项 UI 改动只有类型检查、单元测试和构建产物证据，缺浏览器实测。
- 15 套模板只接入 5 张效果模板；“图1结构+图2风格”因当前每任务只上传一张底图，尚未做风格文字化模板。
- 结构约束只能表达要求，不能保证像素级不变；必须一致的区域仍需原图合成。

## 5. 下一步建议（优先级，2026-09-13 收尾版）

1. **结果验收（用户）**：`1人视_1_生成` 疑似背景建筑与首层结构被改，Codex 建议不采用或重生成；`3半鸟瞰_1_生成` 结构与光感较好。第一轮与复测共产生 4 张新图，均待用户判定。可先做结构对比初查（体块/层数/窗体/相机/背景建筑）再决定。
2. **浏览器实测本轮 UI（未做）**：用隔离 QA（3320/3330、独立 runtime）构造活动旧任务与无扩展两种场景，验证“重新生成”禁用、`lockNotice` 显示与旧版入口可达；不得动生产 runtime。
3. **观察并发绑定缺陷（已加自愈，未复现）**：下次跑双任务时留意第一张是否掉线；若出现，先读扩展 `chrome.storage.local` 的 `binding-diagnostics`（含 `nonce-healed` / `rejected-nonce` / `rejected`）取证，再决定是否深挖。不要把它当作已根治。
4. **官方模板第二批（待定）**：与用户确认是否收录其余专项模板（只改景观、删元素、插人车、分析图、投标单页、透明素材、多轮一致），以及单底图限制下“结构+风格”的文字化方案。
5. **打包发布（未做）**：提交与推送已完成（`387217c`）；若需交付他人，按 AGENTS 做旧项目副本迁移与冒烟后打包。

### 本轮不再执行

- 真实生成验证已随双任务验收完成（用新内置模板实际生成了两轮 4 张图）。
- 提交/推送已完成，工作区干净。

## 6. 关键文件与命令

- 本次新增：`app/01_canvas_app/src/prompt-library-item.ts`、`app/01_canvas_app/test/prompt-starters.test.ts`、`docs/交接记录/`（本文件）。
- 本次修改：`SimpleCanvas.tsx`、`use-simple-project.ts`、`simple-state.ts`、`bridge-client.ts`、`prompt-starters.ts`、`test/simple-state.test.ts`、`app/03_browser_extension/src/background.ts`、`test/multi-tab.test.ts`，以及 3 份提示词相关文档、回归保护基线、AI 接手交接文档、AGENTS.md。
- 以上（连同上一轮未提交的基础画布改动）已随 `0563c23` 提交、`387217c` 同步文档并推送。
- 检查命令：

```powershell
npm.cmd --prefix app run check:canvas
npm.cmd --prefix app run test:canvas
npm.cmd --prefix app test
git diff --check
```

PowerShell 默认禁止 `npm.ps1`，本机需用 `npm.cmd`。

## 7. 现场状态

- 分支 `agent/canvas-simple-prompt-library`，HEAD `0563c23`，已推送且工作区干净；接手若看到未提交改动，属于之后的新工作。
- 桥接 `http://127.0.0.1:3220/health` 正常：`releaseVersion 0.6.0`、`simpleRenderConcurrency 2`、`activeTaskId null`；UI `http://127.0.0.1:3230/` 返回 200。PID 与监听为瞬时信息，接手需现场复核。
- 真实项目 `project_fca8456c-2258-4c4d-be74-249aa0bc406e` 已在同日追加验收中真实写入：revision 1843、节点 12→14，新增 `1人视_1_生成`（task_20260913_16632f0c）与 `3半鸟瞰_1_生成`（task_20260913_d2de4392）。未删除、未取消旧数据。
- `dist` 已随测试构建更新；只改源码不会更新已运行页面，构建后需刷新。

## 8. 追加：真实双任务验收（2026-09-13 下午）

### 8.1 方法与时间线

- 底图：`1人视_1.png`（version_d4e58ccd…）与 `3半鸟瞰_1.png`（version_ac202bbe…）；提示词：新内置模板「明亮晴空 · 清透写实」（两条任务使用同一提示词）。
- 用户在 Chrome 操作，Codex 通过 runtime 文件与 bridge 日志监控。
- 10:05:41 task_20260913_16632f0c 创建（1人视），10:05:46.433 submitted；
- 10:05:46.634 task_20260913_d2de4392 创建（3半鸟瞰），10:05:51.403 submitted；
- 10:06:59 3半鸟瞰完成并回收（68 秒）；
- 10:05:51.4 起 1人视的扩展消息被拒（无事件、无 handoff），10:11:22 用户点「重新绑定当前任务」后立即回收完成。

### 8.2 通过项

- 两个独立 ChatGPT 网页：截图证实两个标签页面板分别绑定 16632f0c / d2de4392。
- 提交串行（相隔约 5 秒）、生成时间重叠，符合默认双任务设计。
- 乱序回图正确：后提交的 3半鸟瞰先完成，先提交的 1人视后完成，各自挂到正确父版本（`parentVersionId` 校验一致）。
- 无重复发送：每个任务仅一次 submitted；结果各 1 张；画布节点 12→14，无重复占位或幽灵节点。
- 无串图：`1人视_1_生成`→16632f0c，`3半鸟瞰_1_生成`→d2de4392。
- 尺寸比例：底图 1440×810，结果 1672×941，比例差约 0.06%，未触发 3% 预警。

### 8.3 失败项（阻断级）

- 1人视任务的扩展绑定在第二张提交的同一秒失效：10:05:51.4 起页面消息被后台以「页面消息与绑定任务或标签页不一致」拒绝；它尝试回收与自动 handoff 均被拒，无法写入 needs-user，任务永久停在 submitted。
- 面板同时显示「人工接管记录失败：页面消息与绑定任务或标签页不一致」；点「重新绑定当前任务」后，同一页面立即恢复并能回收结果。
- 结论：双任务在无人值守下不可用；必须人工兜底。根因未定位，代码层怀疑点在 `background.ts` 的 `bindTab`/`bindingForTab`/`requirePageTask` 与内容脚本持有的 `bindingId` 在第二个网页打开时的竞态，但尚未取得扩展存储对照证据。

### 8.4 画质与结构初查（Codex 目视，非用户验收）

- `3半鸟瞰_1_生成`：体块、层数、窗带节奏、中央通道、草坪形态与底图一致，天空、玻璃反射、绿植、结构阴影明显更真实，观感可用。
- `1人视_1_生成`：构图、相机、两翼体块与中央拱保持较好，但穿拱可见的背景小楼由坡屋顶改为平屋顶竖向构件，首层右侧改为大面积玻璃盒，左立面幕墙分格密度疑似变化；属于超出“只改材质与光影”的结构级改动，建议不采用或重新生成。
- 两条结果均由 ChatGPT 输出 1672×941，非底图原尺寸；如需印刷仍需按既定高清流程处理。

### 8.5 判定

- 双任务排程与归属机制：**走通**。第一次验收因绑定失效需人工兜底后 2/2 回图；同日复测 2/2 全自动回图。
- 无人值守验收：第一次**未通过**；复测**通过**，但缺陷未复现、自愈未触发，属偶发竞态观察中，不能宣称根治。
- 结果采用：**待用户验收**，Codex 不建议直接采用第一次的 1人视结果。

### 8.6 后续处理（同日追加）

- 存储对照：两个 ChatGPT 标签页仍在（578297352 / 578297353），但 `canvas-binding:*` 已随任务完成被正常清理，历史 nonce 不可回看；旧字段全空。
- 推定：重绑能从同一标签页成功，说明该标签页始终绑定同一任务，失败最可能是同标签页内 `bindingId` 过期，而非跨页串任务。
- 已实现窄范围自愈：`requirePageTask` 仅在“同标签页 + 同任务 + 非终态 + 合法生成页 + 有效 nonce”时采用页面 nonce 覆盖过期 nonce；跨页/跨任务仍拒绝。新增 `binding-diagnostics`（最近 20 条）记录 `nonce-healed` / `rejected-nonce` / `rejected`。
- 验证：`npm --prefix app run check`、`npm --prefix app test` 全通过，全仓 182 项（共享 4、桥接 30、扩展 13、画布 135）；扩展集成测试已加入过期 nonce 自愈断言；`app/03_browser_extension/dist/background.js` 已重建。
- **复测结果（同日 10:27—10:28）**：重新加载扩展并刷新画布后，用同样两张底图和同一提示词再跑双任务。批次 `simple_75301918…` 两条任务 `task_20260913_27be8054`（1人视）与 `task_20260913_249a07f8`（3半鸟瞰）均全自动完成：第一张在第二张提交后正常发出「等待当前回复完成」，两张分别自动回收，无需任何人工干预；项目 revision 1958、节点 14→16，各自新增一个版本。
- **关键限制**：扩展存储 `binding-diagnostics` 为空，说明本轮没有发生绑定不匹配，自愈逻辑未被实际触发。因此**缺陷属未复现的偶发竞态，尚不能宣称已修复**；若再次出现卡在 submitted 的现象，先读 `binding-diagnostics` 取证再定位。
