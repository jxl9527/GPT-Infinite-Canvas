# 架构概览

系统由四个相互隔离的核心模块组成：

1. **无限画布**：负责图片、批注、任务角色和版本关系的交互。
2. **本地桥接服务**：负责本地项目数据、任务队列、结果回收和静态文件服务。
3. **浏览器扩展**：负责用户授权下的网页任务发送、状态判断和结果采集。
4. **共享协议包**：统一任务状态、数据结构、错误码和文件命名规则。

关键约束：

- 原始图片只读，所有结果形成新版本；
- 本地服务仅监听 `127.0.0.1`；
- 网页易变逻辑集中在扩展适配层；
- 一次用户操作对应一次生成任务；
- 无法确认网页状态时停止并提示人工接管。
