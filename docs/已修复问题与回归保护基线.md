# GPT Infinite Canvas 已修复问题与回归保护基线

版本：V1.0

建立日期：2026-08-09

适用范围：`D:\OneDrive - 静超科技\D5-AI-Pipeline\canvas`

---

## 一、目的

本文件用于保存画布项目已经修复、已经验证或已经形成测试保护的基础能力。后续三阶段工作流升级不得以重写、删除测试或降低校验强度的方式破坏这些能力。

本基线只记录可由 Git 历史、现有源码、测试和迁移日志验证的内容，不把无法核实的聊天记忆写成既定事实。

---

## 二、证据来源

### 2.1 Git 修复记录

| 提交 | 日期 | 说明 |
|---|---|---|
| `5c309db` | 2026-07-27 | `fix: resolve priority-one canvas reliability issues` |
| `8287311` | 2026-07-27 | `fix: include runtime dependency in v0.3.0 package` |
| `e0b3596` | 2026-08-08 | `feat: ship D5 workflow v2`，包含大量已测试的工作流和可靠性能力 |

### 2.2 迁移验证记录

`MIGRATION_LOG.md` 记录：

- 原项目复制迁入新目录，旧目录保留；
- 153 个项目文件除正常追加日志外，路径、大小和 SHA-256 一致；
- 迁移时共享协议、桥接服务、扩展和画布合计 83 项测试通过；
- D5 Python 管线 18 项测试通过；
- 服务健康检查和画布 HTTP 页面正常。

### 2.3 当前基线验证

2026-08-09 在当前工作区执行：

```powershell
npm test
npm run test:canvas
```

验证结果：

| 模块 | 通过 |
|---|---:|
| 共享协议 | 3 |
| 本地桥接服务 | 15 |
| 浏览器扩展 | 11 |
| 画布应用 | 76 |
| 合计 | 105 |

构建、类型检查和全部测试均通过。该结果作为三阶段改造前的功能基线。

需要特别说明：上述 105 项测试主要覆盖共享协议、本地桥接、浏览器扩展和画布稳定底座，**没有覆盖当前阶段一、阶段二的完整业务流程**。因此：

- 105 项通过只能证明稳定底座未发生已知回退；
- 不能据此判断阶段一、阶段二的入口、输入组织、提示词、结果返回和界面操作保持不变；
- 本次“V3 升级前回归基线”Git 提交用于保存当前代码快照和提供可比较、可回退的版本锚点；
- 阶段一、阶段二在补齐专项测试前，只能通过代码差异、真实项目副本和人工用例进行对照。

---

## 三、不可回归的基础能力

以下能力属于稳定底座，不因三阶段工作流调整而改变。

### 3.1 项目保存与数据恢复

- 自动保存使用最新状态快照，避免并发保存用旧 revision 覆盖新修改；
- 页面隐藏或关闭前尝试刷新未保存状态；
- 保存失败或服务离线时不离开当前画布；
- 项目保存与恢复保持图片节点、批注、版本引用、文字卡和任务关系；
- 未登记资产不能写入项目；
- 项目地址刷新后保持当前项目，返回项目列表时清除项目标识；
- 原始资产只读，生成结果形成新版本；
- 删除画布节点不删除原资产，子节点解除失效父引用；
- 旧项目迁移不得丢失图片、批注、任务、文字卡和父子版本。

主要保护测试：

- `project-state.test.ts`
- `project-navigation.test.ts`
- `project-route.test.ts`
- `server.test.ts`

### 3.2 撤销、重做与画布交互

- `Ctrl+Z`、`Ctrl+Y` 和 `Ctrl+Shift+Z` 正确恢复有限历史；
- 撤销后产生新编辑时丢弃旧重做分支；
- 输入框、可编辑区域和组合键不会误触发删除或画布快捷键；
- 方向键微调图片或批注，`Shift` 支持较大步长；
- 中键拖动画布时文字卡同步移动；
- 鼠标在窗口外松开时结束文字卡拖动或画布漫游；
- 批量框选支持反向拖动和空白处起框；
- 图片浮动工具栏保持在可见安全范围。

主要保护测试：

- `canvas-history.test.ts`
- `canvas-input.test.ts`
- `text-card-pan.test.ts`
- `marquee-selection.test.ts`
- `canvas-layout.test.ts`

### 3.3 图片显示、布局与内存控制

- 导入大图等比缩小，不放大小图；
- 调整显示比例只改变图片框，不改变源图像素；
- 图片框裁切采用居中 `cover`，不修改源图；
- 生成图显示尺寸与元数据不同时，以实际解码尺寸计算；
- 横向、纵向整理使用各图片实际尺寸，避免重叠；
- 生成结果固定放在父图右侧并记录父版本；
- 大量图片使用可见区域加载和有限缓存；
- Blob Object URL 在离开画布时释放，避免长期内存累积。

主要保护测试：

- `canvas-layout.test.ts`
- `canvas-math.test.ts`

### 3.4 任务状态、严格串行与恢复

- 同一时间只允许一个活动任务；
- 已提交任务不能回退到附件上传或准备提交状态；
- 先在服务端记账 `submitted`，再点击网页提交按钮；
- 服务重启后恢复未完成任务、已提交时间和任务锁；
- 人工接管跨服务重启恢复；
- 用户可以结束已经提交的本地任务跟踪，并继续下一任务；
- 已提交到 ChatGPT 的内容不能被本地取消操作误报为已撤回；
- 文字任务可以只保存文字结论并完成，不要求图片结果；
- 任务完成前重新核验结果文件大小和 SHA-256；
- 文件被修改时保持 `collecting`，进入恢复状态；
- 同一 SHA-256 结果去重，不重复写入或回收。

主要保护测试：

- `task-store.test.ts`
- `protocol.test.ts`
- `canvas-automation.test.ts`

### 3.5 自动回收与重复提交保护

- 任务完成后只回收尚未返回画布的结果；
- 生成中、正在回收或已经全部返回时不重复回收；
- 历史图片基线只保存哈希并过滤重复源地址；
- 页面事件必须来自同一任务和同一绑定标签页；
- 只有非终态任务和明确的 ChatGPT 标签页可以主动重绑；
- 自动自愈绑定只适用于正在打开页面的任务；
- 旧扩展版本被拦截，当前最低兼容版本由测试固定；
- 图片任务在正式结果返回前显示生成占位，真实节点返回后隐藏。

主要保护测试：

- `canvas-automation.test.ts`
- `background-guards.test.ts`
- `page-state.test.ts`
- `build-output.test.ts`

### 3.6 登录、验证与浏览器安全

- 只识别合法 ChatGPT HTTPS 页面；
- 自动任务只接受固定本地画布来源；
- 登录或网页验证需要人工处理时才将 ChatGPT 切到前台；
- 普通结果解析失败不应无故抢占前台；
- 页面状态不确定时暂停，不重复提交；
- 不读取 Cookie、密码、ChatGPT 登录凭据或认证 Token；
- 本地服务只监听 `127.0.0.1`；
- HTTP 接口校验本地 Token 和允许的 Origin；
- 诊断输出不得泄露本地桥接 Token；
- 静态服务和资产读取阻止路径越界。

主要保护测试：

- `background-guards.test.ts`
- `build-output.test.ts`
- `server.test.ts`
- `canvas-static-server.test.ts`
- `attachments.test.ts`

### 3.7 文字卡与长文本

- 文字结果在来源图片右侧形成可追溯文字卡；
- 文字卡保留来源版本和任务 ID；
- 优先提取 `【最终生成提示词】` 段落；
- 文字卡可以按画布缩放自由调整尺寸，并保留最小可用尺寸；
- 短审查文字原样保存；
- 长文字在项目字段上限内完整保存，不被意外截断；
- 工作台宽高只恢复有效值，始终受视口安全边界约束。

主要保护测试：

- `text-card.test.ts`
- `workflow-text.test.ts`
- `workbench-panel.test.ts`

### 3.8 项目创建与导航

- 可不上传项目需求 Markdown，直接创建空白项目；
- 有项目需求时仍保存原文件、结构化上下文和生成上下文；
- 回到项目列表前先保存；
- 保存失败或离线时阻止离开，避免数据丢失；
- 项目路由刷新后可恢复，错误项目标识不会误开项目。

该能力源自 `5c309db` 和当前未提交但已测试的导航修复。

### 3.9 发布包完整性

- 发布包必须包含桥接服务运行所需依赖；
- 发布包启动后桥接健康检查可用；
- 画布静态页面返回成功；
- 运行时 Token 正常创建；
- 发布验证检查资产、版本、父子关系、文件大小和 SHA-256；
- 启动脚本不得包含高风险下载或持久化特征。

该能力源自 `8287311`，由以下脚本保护：

- `app/scripts/smoke-release.mjs`
- `app/scripts/verify-release.mjs`
- `app/scripts/package-release.mjs`

---

## 四、阶段一、阶段二测试缺口与补齐要求

当前阶段一、阶段二没有端到端或完整业务行为测试，现有局部单元测试对版本升级的业务对照价值有限。V3 开发不得把这些未覆盖行为误写成“已有自动化保护”。

在修改对应流程前，应优先建立最小行为刻画；确实难以自动化的内容，必须形成带输入样例、操作步骤和预期结果的人工回归记录。

### 4.1 阶段一最低行为基准

- 单个视角只输入一张用户选定的 D5 视角图；
- 不执行同角度多图候选比较；
- 多个不同视角分别创建任务和文字结果；
- 可选 SU 截图只服务于对应 D5 视角的可见细节判断；
- 返回主要问题和明确调整动作，包括焦距、构图、相机高度、俯仰和可见 SU 细节；
- 阶段一只返回文字，不发起生图；
- 可以直接进入阶段一，不依赖其他阶段状态。

### 4.2 阶段二最低行为基准

- 可以跳过阶段一直接导入图片进入阶段二；
- 明确区分结构底图与可选风格参考图；
- 提示词生成与效果图生成是两个独立动作；
- 未经用户明确发起不得自动生图；
- 目标图用于指导 D5 深化，不作为结构依据或最终交付图；
- 多个视角保持独立任务、独立结果和独立追溯关系。

### 4.3 旧规则到 V3 的待建测试映射

下表是 V3 应补充的测试目标，不代表当前已经存在对应自动化测试。

| 旧测试或规则 | V3 等价保护 |
|---|---|
| 阶段一把三个候选视角组成同一个文字任务 | 前置阶段每张 D5 视角生成独立 item 和独立文字卡 |
| 阶段二固定 D5 与 SU 角色 | 前置阶段每个 item 固定一张 D5 主图和可选配对 SU 图 |
| 阶段三为每张底图附带同一风格参考 | 优化阶段每张结构底图独立处理，并可复用共同风格参考 |
| 固定九张工作流提示词卡 | 三阶段模板数量按新规则更新，但结构保护和动作边界测试必须保留 |
| 阶段四玻璃整图 | 最终阶段仍只输入最终 D5 完整图，并保持比例、裁切和结构 |
| 正式归档固定写入 AI 目录 | 改为用户指定文件夹批量导出，同时保留不覆盖、顺延命名、去重和日志 |
| 单一 `selectedVersionId` 推断阶段 | 改为三阶段独立状态槽位，同时保留可验证关系优先、人工覆盖优先 |

补齐测试时，提交说明必须写明：新增测试名称、用户需求依据、保护的行为和仍需人工验证的部分。

---

## 五、三阶段升级的回归策略

### 5.1 修改原则

- 现有底层模块优先复用，不为阶段改名重写任务存储、资产仓库、结果回收或浏览器绑定；
- 新三阶段逻辑优先通过适配层、迁移器和新增字段实现；
- 不使用 `git reset --hard`、`git checkout --` 或清理命令覆盖当前工作区修改；
- 当前未提交修改属于现有工作成果，开始实施前必须先确认并建立可恢复基线；
- 原有测试不得为了新版通过而降低断言强度；
- 业务语义改变的测试必须先增加 V3 等价测试，再移除旧断言；
- 项目 schema 迁移先备份或创建快照，失败时保留旧项目原文件；
- 任何导出仍遵循不覆盖和可恢复原则。

### 5.2 实施前基线动作

1. 保存 `git status --short` 和 `git diff --stat`；
2. 在用户确认后建立基线提交或可恢复补丁；
3. 运行 `npm test`；
4. 运行 `npm run test:canvas`；
5. 记录测试数量、版本号和构建产物；
6. 为阶段一、阶段二建立最小行为刻画测试；暂时无法自动化的行为保存人工回归记录；
7. 使用真实项目副本验证项目打开、保存和恢复；
8. 对项目资产、版本和父子关系生成校验报告。

### 5.3 每个开发阶段的准入条件

```text
类型检查通过
→ 共享协议、桥接、扩展测试通过
→ 画布测试通过
→ 旧项目迁移测试通过
→ 新三阶段专项测试通过
→ 人工完成核心功能冒烟测试
```

任何一步失败，不得继续覆盖或迁移正式画布项目。

### 5.4 发布前必须执行

```powershell
npm test
npm run test:canvas
npm run fixture:release -- <旧项目副本路径>
npm run verify:release -- <隔离验收样本路径>
npm run package:release
npm run smoke:release -- <已解压发布包路径>
```

发布包冒烟测试必须在独立临时 runtime 中运行，不使用正式项目数据。

---

## 六、人工冒烟检查清单

### 项目与保存

- 创建空白项目；
- 创建带 Markdown 项目；
- 导入图片后刷新页面，数据仍在；
- 移动图片和文字卡后返回项目列表，再进入仍保持位置；
- 模拟服务离线，确认不能带着未保存修改离开；
- 打开旧项目副本，图片、文字卡、批注和版本关系完整。

### 画布交互

- 导入多张大图，缩放和漫游流畅；
- 撤销、重做和撤销后新编辑正常；
- 中键漫游、框选、键盘微调和删除不误触；
- 整理图片不重叠，源图尺寸不改变；
- 结果节点位于父图右侧并保留连线。

### 任务与结果

- 文字任务返回文字卡，不要求图片；
- 图片任务显示占位并只回收一次；
- 一个任务运行时不能创建第二任务；
- 登录或验证异常时暂停并可人工接管；
- 结束本地任务后可以创建下一任务；
- 服务重启后活动任务和提交锁恢复。

### 三阶段专项

- 多个不同视角分别创建前置文字任务；
- D5 与 SU 截图一对一配对；
- 可跳过前置阶段直接进入优化阶段；
- 可直接导入最终 D5 进入最终阶段；
- 最终玻璃图比例异常时暂停；
- 只导出用户选定版本，目标目录已有文件不被覆盖。

---

## 七、完成定义

三阶段升级只有同时满足以下条件才算完成：

1. 105 项现有基线测试对应的底层能力全部保留；
2. 阶段一、阶段二已补齐最小行为刻画测试，不能自动化的部分已有人工回归证据；
3. 新三阶段专项测试全部通过；
4. 旧项目副本迁移前后资产、版本和父子关系可核验；
5. 人工冒烟检查通过；
6. 发布包验证与独立 runtime 冒烟通过；
7. 未覆盖或删除用户原始项目数据。

如测试总数发生变化，必须说明新增、替换和移除的具体测试名称，不能只报告“测试通过”。

---

## 八、V3 升级验收记录（2026-08-09）

- 升级前基线：`2fbd96e V3 升级前回归基线`，105 项测试。
- V3 验收：111 项测试通过；共享协议 4、桥接服务 17、浏览器扩展 11、画布应用 79。
- 阶段一、阶段二不再属于“未测试”范围；多视角独立审查、D5／SU 配对、阶段直入、提示词／生图分离已建立行为测试。
- 15 图 schema 1.0 旧项目副本已在真实浏览器中迁移为 schema 2.0，图片、文字卡、版本和视角状态保留。
- 严格 20 图＋批注发布校验、0.4.0 打包和独立 runtime 冒烟均通过。
- 具体测试名称、迁移数据和发布哈希见 `docs\V3测试与验收报告.md`。

---

## 九、V3 对抗式基础审查增量（2026-08-09）

- 审查前当前工作区：116 项测试通过。
- 审查后：122 项测试通过；共享协议 4、桥接服务 17、浏览器扩展 12、画布应用 89。
- 新增 6 项测试，不替换、不移除既有测试：
  - `节点索引同时支持画布 id、版本 id 与父子关系查询`；
  - `批量选择始终保持画布顺序且忽略失效 id`；
  - `大项目索引不会遗漏末尾节点和跨段父子关系`；
  - `画布对象值可无损编码并支持中文 id`；
  - `画布对象导航拒绝未知类型、空 id 与换行注入`；
  - `窄窗口图片工具栏避开左侧固定工具轨`。
- 真实浏览器新增验收：
  - 720×600 下图片工具栏不得被左侧工具轨遮挡；
  - 项目列表内容超过视口时必须由工作台自身滚动；
  - 1440×900 宽屏项目列表无布局回退；
  - 画布主区域按 Tab 可调出对象导航器，并可清空／恢复图片选择；
  - 隐藏文件输入不得成为不可见 Tab 停靠点；
  - 返回项目后 URL 不保留项目标识，刷新不得闪回画布。
- 12,000 节点合成查询基准：旧式重复查找中位 84.17 ms，索引查询中位 1.45 ms，约 58 倍；该数值只用于算法对比。
- 详细修复、截图和暂缓项见任务输出 `canvas_v3_deferred_fixes_20260809_214358\V3对抗式基础审查与修复报告.md`。

---

## 十、真实发图增量回归（2026-08-09）

- 全仓 126 项测试通过：共享协议 4、桥接服务 17、浏览器扩展 12、画布应用 93。
- 新增 4 项测试，不替换、不移除既有测试：
  - `短文件名直接转为视角名并移除D5通道后缀`；
  - `长文件名生成的视角名不超过服务端保存上限`；
  - `截断视角名不会留下不完整的代理字符`；
  - `文字动作移除图片生成输出要求，生成动作保持原要求`。
- 真实 ChatGPT 测试任务 `task_20260809_7f5dfdd4` 已验证：1 张附件上传、页面可见确认、单次自动提交和文字回复均成功。
- 上游回复异常保持“停止回答”后，任务按安全超时进入人工接管；通过现有恢复协议完成后，画布自动生成文字卡并写入视角结论，项目 revision 7 → 8。
- 不可回归规则：客户端与桥接服务必须共享视角名称上限；分析／提示词动作不得携带图片生成输出要求；生图动作仍必须明确返回可见图片。
- 上游 ChatGPT 若仍显示“停止回答”，扩展不得提前回收可能未完成的文字；允许人工停止后使用“立即检查结果”兜底。

---

## 十一、混合框选、批量移动与生成结果排列增量（2026-08-09）

- 全仓 130 项测试通过：共享协议 4、桥接服务 17、浏览器扩展 12、画布应用 97。
- 新增 4 项测试，不替换、不移除既有测试：
  - `同一框选同时包含图片与GPT文字卡`；
  - `批量移动保持图片与文字卡的相对位置`；
  - `同源生成结果在父图右侧纵向排队且不覆盖已有结果`；
  - `生成占位框避开同源已生成图片并进入下一纵向空位`。
- 真实浏览器隔离验收：一次画框选中 1 张图片与 2 张文字卡；拖动任一已选文字卡后，两张文字卡同步移动约 60 × 40 px并保持相对间距。
- 浏览器验收期间拦截项目保存请求，刷新后丢弃测试态；正式项目 revision 保持 298，未写入测试位置。
- 不可回归规则：默认选择工具与批选工具都必须允许图片、GPT文字卡进入同一选区；拖动任一已选图片或文字卡必须整体移动混合选区；同源生成占位和正式回图必须共享父图右侧纵向空位算法，不得覆盖已有结果。

---

## 十二、选择跟随输入、右侧功能轨与提示词交接增量（2026-08-10）

- 全仓 135 项测试通过：共享协议 4、桥接服务 17、浏览器扩展 12、画布应用 102。
- 新增 5 项测试，不替换、不移除既有测试：
  - `上下文工作台只跟随图片选择并区分单选与批选`；
  - `上下文工作台优先放在图片下方并避开右侧功能轨`；
  - `批量阶段提示词保留用户填写的统一任务要求`；
  - `提示词结果卡只使用待确认、已载入和已使用三种交接状态`；
  - `02B提示词载入后生成图片，继续优化则建立带原提示词的02C输入`。
- 真实浏览器验收：
  - 画布恢复后的默认选中状态不自动弹出输入框，用户实际点击图片后才显示跟随输入；
  - 单选图片显示提示词库、本轮输入、阶段、动作与生成目标；多选图片显示统一任务要求和批量发送；选择文字卡时不显示图片输入；
  - 任务预检与最终导出常驻右侧窄功能轨，展开抽屉向左打开且不遮挡功能轨；
  - 720 × 600 下抽屉宽度受视口约束、高度铺满安全区，内容由抽屉内部滚动；
  - 无参考图的文生图／分析图仍可在任务预检抽屉中显式填写，不依赖画布图片选择；
  - 02B 提示词卡可载入原图输入框并标记“已载入输入框”；继续优化会建立含原提示词的 02C 输入，原 02B 卡不被覆盖。
- 浏览器交接动作验收时拦截项目保存请求，验证后的内存测试态随浏览器关闭丢弃；未把提示词交接测试内容写入项目。
- 不可回归规则：底部不得恢复常驻输入框；图片输入必须由真实图片选择触发并跟随选区；多选的统一任务要求必须进入每个批次任务提示词；提示词卡载入不得自动发送；02C 必须建立新输入和新结果卡；右侧功能轨必须持续可达；无图片文生图入口不得因上下文输入改造而丢失。

---

## 十三、文字卡载入后直接显示来源图输入增量（2026-08-10）

- 全仓 136 项测试通过：共享协议 4、桥接服务 17、浏览器扩展 12、画布应用 103。
- 新增 1 项测试，不替换、不移除既有测试：
  - `载入文字卡提示词时清空文字卡选择并直接打开来源图片输入`。
- 修复原因：旧实现选中了来源图片，但仍保留 GPT 文字卡选择；上下文工作台按“存在文字卡选择”隐藏图片输入，导致用户还需再次点击原图。
- 修复后，点击“载入原图输入框”或“继续优化（02C）”会一次完成：清空文字卡与批注选择、切回选择工具、选中来源图、关闭右侧抽屉、显示跟随输入并将焦点放到已载入文字末尾。
- 真实浏览器验收使用 17 图测试项目，保存接口预先拦截；单次点击后可访问性树显示“当前图片输入”，`本轮输入`为 active，测试态未写入项目。
- 不可回归规则：文字卡交接按钮必须是一步完成的原子操作；不得要求用户再点击来源图；载入后不得自动发送任务。

---

## 十四、Codex A/B 双轨工作流增量（2026-08-10）

- 全仓 **144 项测试通过**：共享协议 4、桥接服务 23、浏览器扩展 12、画布应用 105。
- 相对 136 项基线新增 8 项测试，不替换、不移除既有测试：
  - 项目文件夹扫描拒绝磁盘根目录、越界路径和符号链接；
  - 文件夹清单识别 D5 底图、SU 可见细节图和风格参考，并记录 SHA-256；
  - 清单导入只复制已校验文件，源目录保持只读；
  - Codex Run 拒绝未确认授权和超出阶段、动作、项目数、生成上限的请求；
  - 文字意见卡按幂等键写入原图左侧，重复调用不重复创建；
  - 生图提交实行严格串行、租约和尝试次数上限；
  - 生成结果写入项目资产区、建立父子版本并排列在原图右侧，且不自动标记采用；
  - A/B 接管只处理安全检查点，已完成项不重复提交。
- Codex 插件固定提供 12 个受限领域工具；插件清单、MCP 协议探针、Skill 校验及本地 Marketplace 校验全部通过。
- 根项目与插件依赖执行 `npm audit --audit-level=moderate`，均为 0 个漏洞。
- 严格发布校验使用 20 张图片、1 个批注、4 个快照完成资产分层、哈希和恢复验证。
- 独立发布包冒烟通过：桥接服务、画布、连接令牌、12 个 MCP 工具和实时能力探针均正常。
- 最终发布包：`releases\GPT_Infinite_Canvas_0.5.0_v2.zip`；SHA-256：`284cdf9784534a732b748793815962c66123d18edaf6df770403089b935695e0`。
- 真实浏览器隔离验收：A/B 抽屉、只读文件夹扫描、3 张素材导入、B 授权单、批次建立及 720 × 600 窄屏布局均通过；仅存在不影响功能的 `favicon.ico` 404。
- 不可回归规则：A/B 必须共享同一项目和阶段语义；B 必须经授权单运行；源文件不得覆盖；三阶段不得互设门槛；最终选图必须人工确认；结构风险必须暂停；接管不得重复生成；连接发现文件不得保存 Token。

---

## 十五、B 模式双通道导入与阶段二连续运行增量（0.5.1，2026-08-10）

- 全仓 **145 项测试通过**：共享协议 4、桥接服务 23、浏览器扩展 12、画布应用 106；0 失败、0 跳过。
- 项目文件夹入口同时支持：粘贴绝对路径后只读扫描；调用 Windows 系统文件夹选择器并在选择后自动只读扫描。
- 系统选择器由本地 Bridge 打开，不依赖浏览器取得绝对路径；取消选择不清空现有项目内容，不扫描父目录或整盘。
- A 模式阶段二继续保留“生成／优化提示词”“按提示词生成图片”两个独立动作。
- B 模式阶段二固定为一次授权内连续执行“登记提示词卡 → 读取已批准提示词 → 提交生图 → 登记候选图”；提示词卡状态为 `prompt-approved` 中间检查点，不再误把任务提前标记完成。
- Codex MCP 12 个领域工具通过协议探针和正式 0.5.1 Bridge 实时调用；插件已安装到 `d5-ai-canvas@d5-ai-canvas-local` 0.5.1。
- 真实浏览器验收通过双入口布局、MCP 0.5.1 能力显示及 6 图只读清单扫描；未执行导入、建批次或收费生图。控制台仅有不影响功能的 `favicon.ico` 404。
- 最终发布包 `releases\GPT_Infinite_Canvas_0.5.1_v2.zip` 通过隔离冒烟；SHA-256：`9af0f502d3429c74b0cad33b80cf5c22b317229af3fe46e9ea3044a08f049be9`。首包保留为打包历史，`_v2` 包含新版 Codex CLI 重装语法兼容。
- 不可回归规则：浏览器不得伪造本地绝对路径；B 阶段二不得重新暴露动作二选一；批准提示词必须成为实际生图输入；A 模式的分步控制不得被 B 模式自动化覆盖。

---

## 十六、B 模式当前画布直接启动增量（0.5.2，2026-08-10）

- 全仓 **146 项测试通过**：共享协议 4、桥接服务 23、浏览器扩展 12、画布应用 107；0 失败、0 跳过。
- B 模式默认从当前画布开始，不再要求先扫描项目文件夹；现有画布图片可以直接形成 Codex 授权范围。
- 当前画布来源优先级固定为：用户框选图片 → 已准备批次 → 已登记 D5 视角／阶段底图 → 普通导入图，并按 `versionId` 去重。
- 风格参考与 SU 可见细节参考只保留参考职责，不得误作 D5 生图底图；用户需要新素材时仍可切换“项目文件夹”，使用绝对路径或 Windows 文件夹选择器导入。
- 当前画布启动不要求文件夹清单，授权请求允许 `sourceFolder: null`；仍必须保存画布、确认阶段、范围和生成上限后才能建立批次。
- D5 AI Canvas 插件已安装并验证为 `d5-ai-canvas@d5-ai-canvas-local` 0.5.2；12 个 MCP 领域工具通过正式 Bridge 实时调用。
- 真实浏览器验收使用含 6 张已登记 D5 图片的既有项目：切换 B 后默认显示“当前画布”、6 张输入和授权单；未确认授权、未建批次、未生图。证据截图：`output/playwright/qa-0.5.2/b-mode-current-canvas-start.png`。
- 最终发布包 `releases\GPT_Infinite_Canvas_0.5.2.zip` 通过隔离冒烟；大小 6,475,473 字节，包内 3,677 个文件，SHA-256：`cc9a6a9ac4fb4ed3721fd1edcafc644cc64a3a31794083f31e3a7323a39d2ac7`。
- 不可回归规则：B 模式不得重新强制文件夹起步；显式框选必须优先于自动登记范围；参考图不得混入底图；当前画布与文件夹两条入口必须共用同一授权、阶段、串行和人工最终选图规则。

---

## 十七、B 模式结构风险候选自动补生增量（0.5.3，2026-08-10）

- 根因确认：0.5.2 把场景优化候选的 `suspected / confirmed` 结构风险直接写成任务项 `failed`，并在授权单启用结构保护时把整批置为 `paused`；总生成额度虽有剩余，状态机没有同一底图的下一候选分支。
- 0.5.3 将批次生成总额度按任务项公平分配为单图尝试额度；不可整除时余数按画布顺序分配，且总尝试数不越过授权上限。
- 场景优化候选校验失败后保留失败图、父子版本、失败原因和尝试次数；额度尚余时，同一任务项从 `result-registered` 安全回到 `prompt-approved / queued`，随后严格串行补生。
- 单图额度用尽后，该视角保留 `failed / result-registered`，批次继续下一排队视角；不会因首张或单张结构风险候选退出整批。
- 最终玻璃深化继续执行严格策略：结构风险、原尺寸、比例或完整画幅校验失败时立即暂停，不自动补生或绕过人工复核。
- 结构保护提示词升级为“像素坐标上的受控局部编辑”，新增外轮廓、入口净空、柱网、阳台、洞口、幕墙分格、道路交叉口、斑马线、路缘与硬边对齐约束；需要重绘结构的区域宁可保持原样。
- 桥接专项测试扩展了完整恢复链：第一张风险候选自动排队第二张；第二张仍失败且单图额度耗尽后，批次继续第二视角；第二视角通过后批次正常完成。
- 修复 A/B 运行器相互干扰：A 模式的浏览器自动提交与刷新恢复只允许处理 `manual` 运行器，不得把 B 模式 `codex / generation-submitted` 项误判为“活动任务丢失”并暂停；0.5.3 恢复迁移可将这类既有误标项安全恢复为等待登记结果状态。
- 真实批次恢复验收：6 张底图、授权总额度 12、每图 2 次；12 张结构风险候选均被保留，单图额度耗尽后依次推进，最终批次状态为 `completed`，未再次暂停或提前退出。
- 不可回归规则：场景优化的风险候选必须保留且不得自动采用；补生不得覆盖旧候选、不得重建提示词卡、不得改变幂等键、不得越过单图或批次授权额度；最终采用版本仍由设计师人工选择。

---

## 十八、对抗性审查加固与大项目性能增量（0.5.3 工作版，2026-08-11）

- 全仓 **154 项测试通过**：共享协议 4、桥接服务 24、浏览器扩展 12、画布应用 114；0 失败、0 跳过。
- 项目保存启用同修订冲突保护：完全相同的幂等重试允许通过，仅顶层保存时间变化不构成冲突；同一 revision 的实际内容差异和低 revision 写入均拒绝，避免画布整文档覆盖 Codex 已登记结果。
- B 模式阶段二服务端只接受连续 `generate` 合同；前置阶段只接受 review 卡，场景优化只接受 prompt 卡，最终玻璃阶段不接受文字卡，且同一任务项不能用不同幂等键重复登记文字卡。
- `generation-submitted` 与结果登记绑定同一 `leaseOwner`；活动租约拒绝其他执行器抢占。结果登记重新核对结构底图 SHA-256 和已批准提示词 SHA-256，来源或提示词变化时拒绝过期结果。
- 相同像素候选在资产层去重后，重试仍会继续推进当前任务项的校验、额度和终态，不再停留在 `running / generation-submitted`；已终结任务的重复登记保持幂等。
- 活动 Codex 批次禁止切换项目；暂停后允许安全切换。未完成 Codex 批次禁止删除项目，避免结果回收写入错误项目或项目被移入回收区。
- 最终导出必须同时满足：版本存在、版本与资产一致、生成版本父级等于该视角最终 D5 底图、候选已验收通过、候选已由用户选定。画布在调用导出前先保存最终选定状态，伪造版本或未选定资产按单项失败报告。
- 项目文件夹扫描改为流式 SHA-256；超过 40 MiB 的图片直接标记不可导入，不再整文件读入内存。画布恢复先恢复结构，只为视口附近图片排队取图，最大并发 4；Konva 只挂载视口附近和当前保留选择的图片节点。
- 项目 JSON 保存上限由 8 MiB 提升到 32 MiB；画布快照最多保留最新 120 份，后续创建新快照时清理更早的快照，正式项目与源图片不受影响。
- 所有现有单选按钮组统一支持方向键、Home、End 和单一 Tab 停靠点；小字号灰色文字提升到 AA 安全对比度；画布根布局移除 48rem 最小宽度，保留既有窄屏重排规则。
- 发布脚本新增 `--validate-only`，打包版本必须与应用版本一致，并核对桥接服务、启动器、插件运行包、锁文件、插件清单和 MCP 服务共 10 个版本面；发布冒烟以发布清单为准交叉核对应用与插件版本。
- 根项目与插件依赖安全审计均为 0 个漏洞；Codex MCP 12 个工具探针、插件安装校验和发布版本面校验全部通过。本轮未制作或提交正式发布包。
- 不可回归规则：相同 revision 不得覆盖不同项目内容；B 模式不得恢复提示词单独授权；相同候选重试不得卡死；结果登记必须校验租约、底图和提示词哈希；最终导出不得只信任前端 assetId/versionId；项目刷新不得并发拉取全部 4K 原图；任意版本号参数不得绕过内部版本一致性检查。

---

## 十九、视角任务中心与可恢复保存增量（0.5.3 工作版，2026-08-13）

- 全仓 **161 项测试通过**：共享协议 4、桥接服务 25、浏览器扩展 12、画布应用 120；0 失败、0 跳过。未移除或弱化既有测试。
- 自动保存冲突进入可恢复流程：检测到同修订差异后暂停保存，显示更新来源与双方修订号；用户确认后先写入 `canvas/conflicts/` 冲突草稿，再载入当前权威项目。恢复权威版本时跳过无新修订的重复保存，避免冲突面板二次弹出。
- 新增视角任务中心：按视角集中显示阶段底图、候选、采用、风险、验收和导出状态；支持业务名称、关键词及“需处理／待验收／已采用／已导出”筛选，不再以 UUID 或时间戳作为主显示名称。
- 候选工作台支持最多两张同步缩放对比；验收固定为“体块与层数、门窗与幕墙、道路与场地、材质与光影、植物与氛围”五项。结构风险只提供提示，不自动选图；五项全部人工通过后才能采用，拒绝的候选仍保留记录。
- 新建空白项目提供三个可跳过的独立阶段入口；“结构底图、风格参考、最终完整图”角色说明进入视角上下文帮助。两个执行通道统一显示为“浏览器人工批量／Codex 自动运行”，不再向用户暴露 A/B 与 02A/02C 内部编号。
- 右侧视角任务面板支持窄、中、宽三档并持久化；宽屏时画布按实际面板宽度让位，窄屏自动转为受视口约束的覆盖布局。不同工具面板强制互斥显示，避免旧任务预检内容与视角中心叠层。
- 真实浏览器隔离验收完成：三阶段启动器、业务化对象名称、两候选同步对比、五项验收门槛、采用状态、三档宽度、画布让位和冲突草稿恢复均通过；隔离端口与隔离项目目录关闭后已停止，未写入正式项目。
- 验收证据位于 `D:\CodexWorkspace\02_任务\2026-07-30_D5渲染工作流测试项目_投标支持\output\canvas_product_optimization_20260813\ui_qa\evidence`。
- 不可回归规则：冲突恢复不得覆盖本地或权威项目；恢复后不得重复提交同一修订；风险提示不得自动采用候选；五项未全部通过时采用按钮必须禁用；三个阶段入口不得形成跨阶段门槛；任务中心打开时不得同时显示其他抽屉内容；宽屏面板不得遮住同宽度的主画布区域。

---

## 二十、ChatGPT 文字完成状态卡住时自动回收（扩展 1.5.22，2026-08-13）

- 真实故障页已确认：最新助手回复为 2665 字，`【D5调整建议】`、`【最终生成提示词】`、`【必须保持与禁止改变】`三段完整，回复操作按钮已出现，但 ChatGPT 的 `stop-button / 停止回答`仍持续可见。
- 根因是扩展把停止按钮作为绝对生成中信号；即使正文已经完整稳定，`textResponseReady`仍永久返回否，最终在 10 分钟后误转人工接管。
- 扩展 1.5.22 保留正常完成判定，并增加受限恢复分支：仅当任务为文字结果、必需章节全部齐全、最新回复已有完成操作信号、正文连续稳定至少 30 秒且停止按钮仍残留时，才允许自动回收。
- “立即检查结果”作为明确的人工动作，也允许回收满足完整章节和完成操作信号的卡住文字；图片任务不使用该放宽规则。
- 回归断言覆盖：稳定不足 30 秒、缺少必需章节、缺少完成操作信号时均不得走自动恢复；完整稳定文字才允许恢复。
- 全仓 161 项测试继续通过：共享协议 4、桥接服务 25、浏览器扩展 12、画布应用 120；0 失败、0 跳过。
- 不可回归规则：不得仅因停止按钮存在而永久丢弃完整文字；也不得只凭字数或短时静止提前回收流式回复。恢复必须同时满足章节完整、页面完成信号和稳定时间门槛。

---

## 二十一、提示词卡到生图输入的章节隔离（2026-08-13）

- 根因确认：旧提取要求 `【最终生成提示词】`前必须有换行；ChatGPT 页面正文被压成连续文本时无法识别该章节，逻辑回退为载入整张文字卡，导致面向设计师的 `【D5调整建议】`误入生图输入。
- 提示词卡继续完整保留并分栏显示 `【D5调整建议】`、`【最终生成提示词】`和`【必须保持与禁止改变】`，复制全文也不改变。
- “载入提示词与禁止项”改为按章节标题提取，不依赖标题前换行；生图输入只组合 `【最终生成提示词】`与`【必须保持与禁止改变】`，兼容旧卡中的`【禁止项】`、`【禁止改变】`和`【负面约束】`标题。
- 对最后一个禁止章节增加窄范围会话尾句清理：剔除“你喜欢此风格吗？”、“需要我继续……吗？”、“如果你愿意，我可以……”等明确面向用户的征询或邀约，同时完整保留其前方结构约束；不得按问号或最后一句粗暴截断。
- 优化阶段文字任务明确要求写完最后一条禁止约束后立即结束，不得追加提问、征询、总结、评价、邀约或下一步建议。
- “继续优化提示词”仍只使用最终生成提示词正文，不把D5调整建议作为待优化提示词。
- 新增回归测试覆盖标准分行文本、ChatGPT压缩换行文本和禁止项后追加征询三种形态；D5调整建议与会话尾句均不得进入生图输入。
- 全仓 163 项测试通过：共享协议 4、桥接服务 25、浏览器扩展 12、画布应用 122；0 失败、0 跳过。
- 不可回归规则：D5调整建议只服务设计复核；不得发送给图片生成任务。生图任务只接收最终生成提示词和禁止约束。

---

## 二十二、A 模式提示词卡一一对应批量生图（2026-08-13）

- A 模式新增“批量按已返回提示词生成”入口。框选原图后，提交前逐项显示“原图 → 当前视角提示词卡”及就绪状态；任一提示词卡缺失、类型错误、来源错配或缺少生成章节时均禁止启动。
- 每个批次项保存 `inputTextCardId` 和提取后的 `inputPrompt` 快照；队列启动后即使继续编辑其他文字卡，也不会改变已确认任务或造成提示词串图。
- 生图输入继续遵循章节隔离：只冻结并发送`【最终生成提示词】`与`【必须保持与禁止改变】`，不发送`【D5调整建议】`。
- A 模式继续复用浏览器严格串行队列、结果自动回收、父子版本登记和右置排版；B 模式授权与状态机不变。
- 新增 2 项画布行为测试，不替换、不移除既有测试：
  - `A模式可将每张原图与其已返回提示词卡一一配对并冻结独立输入`；
  - `A模式一一对应批次在任一提示词卡缺失或错配时拒绝启动`。
- 全仓 **165 项测试通过**：共享协议 4、桥接服务 25、浏览器扩展 12、画布应用 124；0 失败、0 跳过。
- 真实画布只读冒烟：框选“1人视、2人视、3半鸟瞰”并切换优化阶段后，面板显示 `3/3 已就绪`及三组准确配对；现有 `3/6` 未结束批次使启动按钮保持禁用，未提交任务、未改动正式项目。
- 不可回归规则：A 模式一一对应批次不得退化为多图共用统一提示词；提示词内容必须在批次建立时冻结；缺失或错配时不得静默回退到默认提示词；已有活动批次期间不得建立第二批次。

## 二十三、简化图库与独立双任务（2026-09-12）

- 用户批准的新主流程为固定提示词库、选中底图、粘贴提示词及网页生成；旧阶段工作流保留于兼容入口。
- 新版批次逐项保存任务及原图身份，提示词冻结；最多双任务，上传提交有序、生成等待重叠，旧版仍维持原有单活动任务约束。
- 扩展逐任务保存 tabId 和 bindingId，重复打开复用，回包必须匹配来源标签页和绑定。完成一项不清除其他任务绑定；已提交网页关闭不自动重发。
- 增加 9 项测试，累计 174 项（共享 4、桥接 30、扩展 13、画布 127），未删除旧测试。覆盖调度、防重、身份匹配、保存冲突、内容库备份及同名导出。
- 新版入口检查桥接双任务能力，禁止新前端连接旧桥接直接提交。
- 真实 ChatGPT 生图自动化最终验收尚未执行；隔离回包测试、扩展后台模拟和用户已完成的双网页能力实测分别记录，不混为完整实测。

## 2026-09-12 简化版画布基础操作恢复

- 对照 `40e3b60`、`dc63b41` 等旧版交互更新及本基线恢复基础画布，简化版渲染排程与浏览器扩展不变。
- 中键、空格＋左键、平移工具均能漫游；图片命中不拦截中键，窗口失焦与画布外松开结束拖动。
- 反向框选和组合键增减选区支持图片、文字卡、批注；混合拖动、方向键微调（Shift 为 10）、删除、撤销/重做。
- 恢复文字、箭头、画笔、矩形、颜色、对象列表、锁定、图片及文字卡尺寸调整、图片框比例、重命名、原图下载。
- 横向/纵向整理包含混合对象；自动排版复用旧版图片父子分栏及文字卡归属逻辑，批注保持原位置。
- 支持滚轮定位缩放、缩放按钮、100% 和适合画面；键盘操作排除输入框、编辑区、组合输入。
- 原图资产、版本和任务引用保留，基础操作不创建渲染任务。
- 验证：画布 132 项、共享 4 项、桥接 30 项、扩展 13 项测试全部通过。独立浏览器完成中键、空格、混合框选/移动、微调、撤销重做、删除恢复、刷新恢复、批注、尺寸、比例、缩放、排版、画布外释放、下载和输入保护回归。
- 新增 `basic-canvas-model.test.ts` 保护中键优先级、混合对象几何、删除保留引用、尺寸下限、父子分栏和锁定对象；原测试未删除或放宽。
- 复测脚本和旧项目副本位于 `D:\CodexWorkspace\02_任务\2026-09-12_画布基础功能恢复\qa`。未制作正式发布包。

## 2026-09-13 无扩展时"重新生成"的连接一致性

- 根因：简化画布已为"开始渲染"增加扩展连接检查，但队列中的"重新生成"仅重排任务，未做连接判断；未连接时点击后仍会入队并静默等待，用户看不到原因。
- "开始渲染"与"重新生成"共用同一条连接提示文案，按"未连接"和"版本低于 1.6.0"分别说明处理方式，避免两处提示漂移。
- 扩展未连接或版本不足时禁用"重新生成"按钮，并通过 `title` 与队列内 `role=status` 文本给出原因；连接恢复且刷新页面后按钮自动可用。
- 仅调整前端可用性与提示，不改动 `use-simple-project.ts` 的排程、桥接能力和扩展协议。
- 验证：`check:canvas`、`test:canvas`、全仓测试通过（共享 4、桥接 30、扩展 13、画布 132），`git diff --check` 干净。未执行真实 ChatGPT 生图实测，未制作发布包。
- 不可回归规则：无扩展时"开始渲染"和"重新生成"必须同时保持不可用并给出原因；不得以静默入队代替连接提示。

## 2026-09-13 TASK_LOCKED 不再静默、遗留任务入口可见

- 根因：简化批次轮询在创建任务时把桥接的 `TASK_LOCKED` 直接吞掉。若项目里仍有旧的 manual/Codex 任务占用生成名额，或旧版 `batchRun` 未结束，排队项会永远停在"排队中"且没有任何提示。
- 新增纯函数 `simpleBlockingTask(batch, tasks)`：只要存在不属于当前批次且未终结（非 completed/failed/cancelled）的任务，即视为占用生成名额的阻塞项；同批次自己的在跑任务不算阻塞。
- 轮询在创建任务前先检查阻塞项：命中则写入 `lockNotice`（含任务 id 与"打开旧版兼容工作台"入口）并暂缓创建；创建成功后清空，批次完成或全部结束后也清空。
- 仍会捕获并发竞态下桥接返回的 `TASK_LOCKED`，改为显示服务端原文而不是丢弃。
- 队列面板在 `lockNotice` 非空时以 `role=status` 显示原因；文案含"旧版"时附"打开旧版兼容工作台"按钮，`openLegacy` 从设置页内联逻辑提取为共用函数。
- 只调整前端提示与旧版入口可达性，不改桥接的占用规则和排程算法。
- 新增 1 项测试 `占用生成名额的非本批任务被识别为锁，完成或取消后解除`，不替换、不移除既有测试。
- 验证：全仓 **180 项测试通过**：共享 4、桥接 30、扩展 13、画布 133，0 失败；`check:canvas`、`test:canvas`、`git diff --check` 通过。未执行真实 ChatGPT 生图实测，未制造或修改真实任务，未制作发布包。
- 不可回归规则：创建任务遇到非本批活动任务或桥接 `TASK_LOCKED` 时必须可见提示并给出旧版入口，不得静默吞掉后长期等待；同批次自身的在跑任务不得被误报为阻塞。

## 2026-09-13 内置图库接入 GPT Image 2.5 官方骨架

- 来源：用户此前提出的“官方提示词模板结合项目”。对话 `6aa3572c-2e88-83ec-9e25-ea92458edd40`（《查找官方模板》）里的 15 套模板并非官方直接发布的建筑模板，而是依据 OpenAI GPT Image 2.5 Prompting Guide 改写的建筑适配版；上一轮 Codex 已完成适配判断但未实现。本次从 2026-09-11 Codex 会话日志恢复内容并存档到 `D:\CodexWorkspace\02_任务\2026-09-13_官方提示词模板结合评估`。
- 采用上一轮适配判断：复制内容必须是完整自包含提示词（新会话不认识 `STYLE-01` 等编号）；每个任务只上传一张底图，风格迁移模板不引入第二张上传图；局部修改不得改变天气、材质分区、景观和相机。
- 内置基础模板由 6 张调整为 8 张：五张效果渲染模板（明亮晴空 · 清透写实、D5 质感强化、玻璃质感优化、阴天柔光、蓝调夜景）改写为 `REFERENCE / CHANGE ONLY / PRESERVE EXACTLY / VISUAL TARGET / CONSTRAINTS` 骨架，结构保护直接写入提示词；分析图、总平、室内三张沿用原内容。暂不做 L1/L2/L3 选择器。
- 边界：强结构约束能表达要求，但不能保证像素级不变；必须完全一致的区域仍需以原图合成，官方指南同样如此建议。
- `PromptLibraryItem` 类型从仅浏览器可用的 `bridge-client.ts` 抽到节点安全的 `prompt-library-item.ts`，`bridge-client.ts` 继续 re-export，避免测试引入 `import.meta.env`。
- 新增 2 项测试，不替换、不移除既有测试：`内置图库首批五张效果模板为自包含完整提示词`、`图库模板标题与提示词长度满足导入与卡片约束`。
- 验证：全仓 **182 项测试通过**：共享 4、桥接 30、扩展 13、画布 135，0 失败；`check:canvas`、`test:canvas`、`git diff --check` 通过。未做真实 ChatGPT 回图与画质验收，未在浏览器逐张点选复制，未制作发布包。
- 不可回归规则：内置效果模板必须是可直接复制使用的完整提示词，不得只给编号或依赖项目知识库；玻璃质感模板只改玻璃并保留天气、材质分区、景观与相机；结构约束不得被描述为像素级锁定保证。

## 2026-09-13 并发双任务绑定失效的自愈与诊断（缺陷未复现，观察中）

- 真实双任务验收发现：并发打开第二个 ChatGPT 网页后，第一张任务的页面消息被扩展后台以“页面消息与绑定任务或标签页不一致”拒绝；自动回收与自动 handoff 均失败，任务永久停在 submitted，只能靠人工“重新绑定当前任务”兜底（详见交接记录第 8 节）。
- 存储对照显示任务完成后绑定键被正常清理，历史 nonce 不可回看；结合“重绑成功”推定当时该标签页仍绑定同一任务，失败最可能来自同标签页内 `bindingId` 过期，而不是跨页串任务。
- 在 `background.ts` 的 `requirePageTask` 增加窄范围自愈：仅当绑定存在、`binding.tabId === sender.tab.id`、任务非终态、页面为合法生成页且消息携带有效长度 nonce 时，采用页面当前 nonce 覆盖同标签页的过期 nonce；跨标签页、跨任务、非法 URL 仍一律拒绝。
- 增加 `binding-diagnostics` 本地诊断记录（保留最近 20 条），记录 `nonce-healed`、`rejected-nonce` 与 `rejected` 及其任务、标签页、URL 与双方 nonce，供下次复测取证；诊断写入失败不得阻断任务处理。
- `isTrustedTaskMessage` 守卫函数本身不变，跨任务与跨标签页的拒绝测试不变；扩展集成测试 `真实后台逻辑：双网页独立绑定…` 增加同标签页过期 nonce 自愈与自愈后继续可用的断言。
- 验证：`npm --prefix app run check` 与 `npm --prefix app test` 全通过，全仓 182 项（共享 4、桥接 30、扩展 13、画布 135），0 失败；构建产物 `app/03_browser_extension/dist/background.js` 已含 `nonce-healed` 与 `binding-diagnostics`。
- **复测结果（2026-09-13 同日）**：重新加载扩展后同图同提示词再跑双任务，批次 `simple_75301918…` 两条任务全自动完成（`27be8054`/`249a07f8`），第一张未再掉线，项目 14→16 节点、归属正确。扩展存储 `binding-diagnostics` 为空，说明本轮未发生绑定不匹配，自愈未被触发。
- **限制**：该缺陷仍是未复现的偶发竞态；自愈逻辑只在扩展集成测试中验证过，不得把它当作“并发绑定缺陷已解决”。再次出现卡在 submitted 时，先读取 `chrome.storage.local` 的 `binding-diagnostics` 与两个标签页绑定对照，再定位根因。
- 不可回归规则：自愈只允许同标签页、同任务、非终态、合法生成页；不得放宽跨标签页或跨任务的事件校验；诊断记录不得包含令牌或页面凭据。

## 2026-09-13 同图多版本、三任务并发与滑动对比（自动测试 + 真实三任务 + 浏览器交互验收）

- 依据 [图片滑动对比与多版本三任务并发计划书](图片滑动对比与多版本三任务并发计划书.md) 实施；实施记录见 `docs/交接记录/2026-09-13_图片对比与多版本三并发实施.md`。
- **同图多版本**：`SimplePreferences.copiesPerImage`（1/2/3，默认1）与并发数分开设置；`makeSimpleBatch` 按轮次展开（A1、B1、A2、B2），每张底图独立 item 与稳定 `variantOrdinal`；展开后超过 200 个任务时明确报错，不静默截断。
- **活动批次追加**：`appendSimpleVariant` 复用批次冻结提示词，继续编号并记录 `clonedFromItemId`；暂停中追加保持排队，不自动恢复。失败/停止项补生改为 `retrySimpleItem` 新建 item（`retryOfItemId`）并把旧项标记 `supersededByItemId`，保留失败记录；批次完成与调度跳过已替代项。
- **独立占位槽**：`pendingSlots` 按同源待处理顺序分配槽位，`simpleSlotBounds` 计算落点，`appendImage` 接受 `bounds` 且与现有节点重叠时自动回退到 `nextChildBoundsToRight`；乱序回图不会占用其他任务槽位或覆盖已有节点。占位与队列显示 `V序号`。
- **三任务并发**：共享 `SimpleRenderConcurrency = 1 | 2 | 3`；解析、项目保存、批次校验同步接受 3，`TaskStore` 容量判断保持“已占槽任务计入、超过并发即拒绝”；桥接健康声明 `simpleRenderConcurrency: 3`，前端启动检查接受 2 或 3，桥接为旧版（2）时隐藏“3 个任务”选项。默认仍为 2，旧项目设置不变。
- **滑动对比**：`comparison-model` 严格按 `parentVersionId` 配对，支持切换“最初原图”、兄弟版本切换与比例不一致提示；`ComparisonOverlay` 提供 0—100% 分界滑块、键盘方向键/Home/End、适合窗口/100%、滚轮缩放、中键平移、指针与失焦兜底、焦点限制与 Esc 关闭。生成图单击（移动 ≤5px、无组合键、选择工具）延迟 240ms 打开对比，双击预览被保留；工具栏提供“与原图对比”。缺少父版本或资产时降级为单图预览并提示“缺少对比底图”。`onPreview` 现传 `{assetId,name,versionId}`。
- **多结果**：同一任务返回多张图片时按 `_1`、`_2` 子序号命名，全部保留；重复回调按 `taskId+resultId` 幂等。
- 新增测试（不删除、不放宽既有断言）：`同图多版本按轮次展开并分配稳定序号`、`活动批次追加版本继续编号并保持冻结提示词`、`失败项补生建立新任务且不覆盖旧记录`、`同源占位槽位依次递增并排除已完成与已替代项`、`结果按预留槽位向下排列，槽位重叠时判定为不可用`、`三任务服务锁：前三项生成、第四项等待、释放后补位`、`对比默认取直接父版本，可切换到最初原图`、`缺少父版本或资产时对比降级返回空`、`版本切换只包含同父版本的兄弟结果`、`滑块百分比限制在 0 到 100`；`并发合同` 改为接受 1—3、拒绝 4；`simple-persistence` 的越界值由 3 改为 4。
- 验证：`npm --prefix app run check`、`npm --prefix app run check:canvas`、`npm --prefix app test` 全通过，全仓 **193 项**（共享 4、桥接 31、扩展 13、画布 145），0 失败；构建产物 `app/01_canvas_app/dist` 已含“同样设置再生成”“与原图对比”“每张图生成”“3 个任务”“本次底图”“画幅比例不同”“缺少对比底图”等新文案。
- 真实与交互验收（2026-09-13）：同图三份（批次 `simple_eb011b4e…`，三任务 69 秒重叠、乱序完成、同父归属）、三图各一份（批次 `simple_beeff98d…`，49 秒重叠、各自归属、无串图）、活动中追加与第四项排队（批次 `simple_5578755a…`，V4 等 V1 完成才启动，草稿修改未进入任何任务提示词）全部通过；浏览器交互经用户实测，双击与单击冲突已修复并加 `basicCompareClick` 回归测试。证据见 `docs/交接记录/2026-09-13_图片对比与多版本三并发实施.md` 第 8 节。
- **限制**：旧项目迁移冒烟、生成中刷新/网页刷新/扩展重载恢复、真实多结果子序号、生成图画质逐张评审未做；不得宣称计划第 7 节全部场景已验收。
- 不可回归规则：同一请求必须保持 `batchId+itemId` 幂等，主动再次生成只能新建 item；批次提示词冻结后不得被草稿改写；并发上限由服务端互斥容量做最终判定；结果必须按 `parentVersionId` 归属且不覆盖用户移动过的节点；对比配对不得按名称或邻近位置猜测；并发档位在活动批次中不可更改。

## 2026-09-13 画布布局精简与首页删除

- 依据 [画布布局精简与生成参数前置计划书](画布布局精简与生成参数前置计划书.md) 实施；记录见 `docs/交接记录/2026-09-13_画布布局精简实施.md`。
- 画布页只保留单层约 48px 项目栏（品牌导航层不再输出）；右侧改为一行紧凑选图上下文（单选文件名、多选可展开），未选但有批次时显示“本轮任务”。
- 生成参数前置：提示词框下方两列「同时生成 / 每张图生成」，设置页删除重复项与长说明；活动批次期间禁用并显示原因；成功提示 4 秒自动消失，保存/连接/锁定等错误持久可见。
- 左侧竖排工具栏：分组图标（选择/平移｜文字/箭头/画笔/矩形/颜色｜撤销/重做/适合画面｜整理菜单），`title` 含中文名与快捷键，整理菜单向右弹出；点击工具不触发底层框选或图片对比；适合画面把工具轨与底部条计入可视区。
- 按用户追加：顶部导入入口移入「设置」面板（选择图片文件/导入文件夹），原「导出 / 设置」更名为「设置」；首页项目列表新增行内软删除（`deleteWorkbenchProject` → `runtime/trash/projects`，行内确认，删除当前打开项目先解除绑定，`原有项目` 不显示删除，活动任务由服务端拒绝）；主界面字体改为 `Noto Serif SC`（正文 500、标题 600）；底部缩放与提示整组居中、对象列表固定右下。
- 验证：`check:canvas`、`test:canvas`、全仓 **193 项**（共享 4、桥接 31、扩展 13、画布 145）与 `build:canvas` 通过，`git diff --check` 干净；浏览器布局/字体/底部位置经用户目测认可。
- **限制**：未做 1366×768 与 720px 窄屏专项、图库往返、删除后回收恢复演练与截图归档；旧版兼容工作台字体未改。
- 不可回归规则：排版调整不得改变排程、多版本、三并发、对比与扩展协议；删除必须走软删除并保留可恢复路径；`原有项目` 与存在活动任务的项目不得删除；参数控件必须复用 `prefs.concurrency`/`prefs.copiesPerImage` 与 `updatePreferences`，不建立第二份状态。
