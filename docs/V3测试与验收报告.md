# 画布 V3 测试与验收报告

## 验收结论

V3 三阶段升级通过自动化回归、旧项目副本迁移、真实浏览器人工冒烟、严格发布校验和独立发布包启动冒烟。升级前 105 项稳定底座未减少，当前共 111 项测试通过。

## 自动化测试

| 范围 | 通过数 | 主要保护能力 |
|---|---:|---|
| 共享协议 | 4 | 三阶段、输出类型、D5／SU 配对边界 |
| 桥接服务 | 17 | 项目校验、任务安全、最终玻璃批量导出 |
| 浏览器扩展 | 11 | 绑定、提交锁、恢复、结果去重 |
| 画布应用 | 79 | 稳定底座、迁移、三阶段动作与界面状态 |
| 合计 | 111 | 全部通过，0 跳过、0 失败 |

执行命令：

```powershell
npm run check:canvas
npm test
npm run test:canvas
```

## 旧测试与 V3 保护映射

| 旧语义 | V3 等价保护 |
|---|---|
| 阶段一多图比较 | 多个不同 D5 视角拆成独立前置任务 |
| 阶段二单独 SU 审查 | 每张 D5 可选一张 SU，一一配对且不串图 |
| 阶段三生成 | 优化阶段提示词与生图两个独立动作 |
| 阶段四玻璃质量门 | 最终 D5 必做整图玻璃深化，改为结果人工验收 |
| 九张固定提示词 | 七张 V3 固定提示词，围绕三阶段和继续执行 |

新增或替换后的关键测试名称：

- `前置阶段把多个不同D5视角拆成独立文字任务`
- `前置阶段为每个D5视角明确配对可选SU截图`
- `优化阶段可直接进入且提示词与生图保持两个独立动作`
- `最终阶段可直接进入且每张最终D5只形成一个整图玻璃任务`
- `三阶段只开放当前阶段的有效动作且不存在前序完成门槛`
- `旧四阶段项目迁移为V3三阶段且保留项目数据`
- `最终玻璃成果批量导出顺延命名、去重并记录日志`
- `批量导出保留成功项并报告单项失败`

## 旧项目副本迁移

- 输入：schema 1.0 历史项目副本。
- 迁移前：15 张图片、5 张文字卡、7 个视角状态。
- 迁移后：schema 2.0；图片、文字卡和视角数量不变；7 个视角均具有四组 V3 嵌套状态。
- 迁移后状态：旧阶段三统一映射为 `scene-optimization`。
- 原项目未修改；迁移副本位于 `app/output/v3-migration-smoke-20260809-022112`。

## 真实界面冒烟

Playwright 在隔离端口和旧项目副本上验证：

- 旧项目可进入并完整显示图片、文字卡和父子关系；
- 阶段一显示逐行 D5／SU 可选配对；
- 可直接选择阶段二，并分别选择“生成／优化提示词”“按提示词生成图片”；
- 可直接选择阶段三，唯一生成动作是整图玻璃深化；
- 最终导出区只接受人工验收并选定的版本，目录由用户指定。

截图：

- `app/output/playwright/v3-old-project.png`
- `app/output/playwright/v3-final-stage-direct-entry.png`
- `app/output/playwright/v3-final-export.png`

## 发布验收

严格发布校验保留“至少 20 图＋1 条批注”门槛。使用隔离派生样本验证 schema、节点唯一性、版本、父版本、任务链接、资产分层、文件大小、SHA-256、快照、正式构建和启动脚本安全特征，结果全部通过。

```powershell
npm run fixture:release -- <旧项目副本>
npm run verify:release -- <隔离验收样本>
npm run package:release
npm run smoke:release -- <已解压发布包路径>
```

发布包：`releases/GPT_Infinite_Canvas_0.4.0_v3.zip`

- 文件数：142
- 大小：797,134 字节
- SHA-256：`bc73687e3c97cdca0f5573a000898756cf5c6f3cb59baa2cb7ee274d46ea8f70`
- 独立 runtime 冒烟：Bridge 3320 正常；Canvas 3330 返回 200；令牌文件成功生成。

## 已知说明

- `verify:release` 不带参数时仍检查当前默认项目；该项目只有 2 张图、无批注，因此不满足历史发布样本门槛。V3 未降低门槛，而是提供 `fixture:release` 生成不覆盖原文件的隔离验收样本。
- 未执行真实 GPT 提交与收费生成；任务协议、动作组合、回收、暂停和结果导出由自动测试覆盖，界面选择由浏览器冒烟覆盖。
